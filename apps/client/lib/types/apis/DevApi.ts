/* tslint:disable */
/* eslint-disable */
/**
 * Riven Core API
 * API documentation for Riven Core services.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  DevSeedResponse,
  TemplateInstallationResponse,
} from '../models/index';
import {
    DevSeedResponseFromJSON,
    DevSeedResponseToJSON,
    TemplateInstallationResponseFromJSON,
    TemplateInstallationResponseToJSON,
} from '../models/index';

export interface ReinstallTemplateRequest {
    workspaceId: string;
    templateKey: string;
}

export interface SeedWorkspaceRequest {
    workspaceId: string;
}

/**
 * 
 */
export class DevApi extends runtime.BaseAPI {

    /**
     * Dev-only. Removes the existing template installation record (if present) and re-runs installation for the provided template key. Existing workspace entity types with matching keys are reused; missing types are created along with their relationships and semantic metadata.
     * Re-install a template into a workspace
     */
    async reinstallTemplateRaw(requestParameters: ReinstallTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TemplateInstallationResponse>> {
        if (requestParameters['workspaceId'] == null) {
            throw new runtime.RequiredError(
                'workspaceId',
                'Required parameter "workspaceId" was null or undefined when calling reinstallTemplate().'
            );
        }

        if (requestParameters['templateKey'] == null) {
            throw new runtime.RequiredError(
                'templateKey',
                'Required parameter "templateKey" was null or undefined when calling reinstallTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/dev/seed/workspace/{workspaceId}/template/{templateKey}/reinstall`;
        urlPath = urlPath.replace(`{${"workspaceId"}}`, encodeURIComponent(String(requestParameters['workspaceId'])));
        urlPath = urlPath.replace(`{${"templateKey"}}`, encodeURIComponent(String(requestParameters['templateKey'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TemplateInstallationResponseFromJSON(jsonValue));
    }

    /**
     * Dev-only. Removes the existing template installation record (if present) and re-runs installation for the provided template key. Existing workspace entity types with matching keys are reused; missing types are created along with their relationships and semantic metadata.
     * Re-install a template into a workspace
     */
    async reinstallTemplate(requestParameters: ReinstallTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TemplateInstallationResponse> {
        const response = await this.reinstallTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Populates the workspace with realistic mock entities and relationships based on its installed template. Idempotent — returns early if data already exists.
     * Seed workspace with mock entity data
     */
    async seedWorkspaceRaw(requestParameters: SeedWorkspaceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DevSeedResponse>> {
        if (requestParameters['workspaceId'] == null) {
            throw new runtime.RequiredError(
                'workspaceId',
                'Required parameter "workspaceId" was null or undefined when calling seedWorkspace().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/dev/seed/workspace/{workspaceId}`;
        urlPath = urlPath.replace(`{${"workspaceId"}}`, encodeURIComponent(String(requestParameters['workspaceId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DevSeedResponseFromJSON(jsonValue));
    }

    /**
     * Populates the workspace with realistic mock entities and relationships based on its installed template. Idempotent — returns early if data already exists.
     * Seed workspace with mock entity data
     */
    async seedWorkspace(requestParameters: SeedWorkspaceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DevSeedResponse> {
        const response = await this.seedWorkspaceRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
