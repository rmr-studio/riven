// workspace/responses.ts
// Re-export workspace response types from generated code

// Note: Workspace API responses typically return the Workspace model directly
// or void for delete operations. No separate response types are generated.
// Import Workspace from "./models" for response typing.

export {};
