package riven.core.service.customsource

import org.junit.jupiter.api.Disabled
import org.junit.jupiter.api.Test

/**
 * Phase 2 Wave 0 scaffold. Populated by plan 02-03 (see VALIDATION.md).
 * Covers: SEC-01 (SSRF blocklist), SEC-02 (DNS-rebinding-safe resolved-IP check).
 */
@Disabled("Phase 2 scaffold — populated by downstream plan")
class SsrfValidatorServiceTest {
    @Test fun placeholder() {}
}
