package riven.core.service.customsource

import org.junit.jupiter.api.Disabled
import org.junit.jupiter.api.Test

/**
 * Phase 2 Wave 0 scaffold. Populated by plan 02-04 (see VALIDATION.md).
 * Covers: SEC-03 (read-only role enforcement on connect).
 */
@Disabled("Phase 2 scaffold — populated by downstream plan")
class ReadOnlyRoleVerifierServiceTest {
    @Test fun placeholder() {}
}
