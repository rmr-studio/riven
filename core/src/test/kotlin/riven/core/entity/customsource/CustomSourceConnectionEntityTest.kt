package riven.core.entity.customsource

import org.junit.jupiter.api.Disabled
import org.junit.jupiter.api.Test

/**
 * Phase 2 Wave 0 scaffold. Populated by plan 02-01 (see VALIDATION.md).
 * Covers: CONN-01 (CustomSourceConnectionEntity JPA mapping + toModel()).
 */
@Disabled("Phase 2 scaffold — populated by downstream plan")
class CustomSourceConnectionEntityTest {
    @Test fun placeholder() {}
}
