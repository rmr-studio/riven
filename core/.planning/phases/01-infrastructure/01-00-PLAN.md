---
phase: 01-infrastructure
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/test/kotlin/riven/core/service/workflow/ExecutionQueueGenericizationTest.kt
  - src/test/kotlin/riven/core/service/identity/IdentityInfrastructureIntegrationTest.kt
  - src/test/kotlin/riven/core/enums/integration/SourceTypeTest.kt
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06]

must_haves:
  truths:
    - "Test scaffolds exist and compile before implementation begins"
    - "Each test file contains failing test stubs that will pass once the corresponding implementation plan executes"
  artifacts:
    - path: "src/test/kotlin/riven/core/service/workflow/ExecutionQueueGenericizationTest.kt"
      provides: "Regression test scaffold for queue genericization"
      contains: "ExecutionQueueGenericizationTest"
    - path: "src/test/kotlin/riven/core/service/identity/IdentityInfrastructureIntegrationTest.kt"
      provides: "Integration test scaffold for identity tables, constraints, and indexes"
      contains: "IdentityInfrastructureIntegrationTest"
    - path: "src/test/kotlin/riven/core/enums/integration/SourceTypeTest.kt"
      provides: "Unit test scaffold for SourceType enum"
      contains: "SourceTypeTest"
  key_links:
    - from: "src/test/kotlin/riven/core/service/workflow/ExecutionQueueGenericizationTest.kt"
      to: "src/main/kotlin/riven/core/service/workflow/queue/WorkflowExecutionQueueService.kt"
      via: "Tests queue service behavior after genericization"
      pattern: "WorkflowExecutionQueueService"
    - from: "src/test/kotlin/riven/core/service/identity/IdentityInfrastructureIntegrationTest.kt"
      to: "db/schema/01_tables/identity.sql"
      via: "Integration tests validate schema constraints at runtime"
      pattern: "match_suggestions"
---

<objective>
Create Wave 0 test scaffolds for all infrastructure requirements. These are the test files identified in VALIDATION.md that must exist before implementation begins so that behavioral verification is in place for INFRA-01 through INFRA-06.

Purpose: The Nyquist rule requires every verify command to have an automated test. Implementation plans (01-01, 01-02) verify via `./gradlew build` and `./gradlew test`, but several behavioral properties (dedup silent skip, CHECK constraint rejection, GIN index existence, enum value presence) have no existing test coverage. These scaffolds ensure those behaviors are proven at runtime, not just by grepping SQL files.

Output: Three test files with test stubs covering queue genericization regression, identity schema integration, and SourceType enum validation.
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure patterns -->

From src/test/kotlin/riven/core/service/workflow/ (existing test pattern):
```kotlin
@SpringBootTest(classes = [WorkflowExecutionQueueService::class, ...])
@ExtendWith(MockitoExtension::class)
class WorkflowExecutionQueueServiceTest {
    @MockitoBean lateinit var executionQueueRepository: ExecutionQueueRepository
    // ... uses @WithUserPersona for auth context
}
```

From src/test/kotlin/riven/core/service/integration/ (integration test pattern with Testcontainers):
```kotlin
@ActiveProfiles("integration")
@SpringBootTest
// Uses @DynamicPropertySource with PostgreSQL Testcontainers
```

From src/test/kotlin/riven/core/service/util/factory/ (test factory pattern):
```kotlin
// Factory classes provide entity construction for tests
// Never construct JPA entities inline — always use factory methods
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExecutionQueueGenericizationTest scaffold</name>
  <files>
    src/test/kotlin/riven/core/service/workflow/ExecutionQueueGenericizationTest.kt
  </files>
  <action>
    Create `src/test/kotlin/riven/core/service/workflow/ExecutionQueueGenericizationTest.kt` as a unit test scaffold using `@SpringBootTest` with targeted classes loading `WorkflowExecutionQueueService` and its security config. Mock all dependencies with `@MockitoBean`.

    The test class should contain these test stubs (annotated with `@Test` and `@Disabled("Wave 0 scaffold — enable after 01-01 implementation")`):

    1. **`existing workflow queue service creates queue entry with WORKFLOW_EXECUTION job type`** — verifies that when a workflow execution is enqueued, the saved entity has `jobType = ExecutionJobType.WORKFLOW_EXECUTION` and `workflowDefinitionId` is non-null.

    2. **`claim pending executions only returns WORKFLOW_EXECUTION jobs`** — verifies the repository query filters by job type so the workflow dispatcher never claims identity match jobs.

    3. **`queue entry with null workflowDefinitionId is valid for IDENTITY_MATCH jobs`** — verifies that constructing an ExecutionQueueEntity with `jobType = IDENTITY_MATCH`, `entityId = someUUID`, and `workflowDefinitionId = null` is valid and `toModel()` succeeds.

    Use `@WithUserPersona` at class level. Follow existing test conventions: `mockito-kotlin` `whenever`/`verify`, factory classes for entity construction (create or extend a factory if needed for ExecutionQueueEntity).
  </action>
  <verify>
    <automated>cd /home/jared/dev/worktrees/identity-resolution/core && ./gradlew compileTestKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ExecutionQueueGenericizationTest.kt exists with 3 disabled test stubs
    - Test file compiles successfully
    - Tests are disabled so they do not fail before implementation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IdentityInfrastructureIntegrationTest and SourceTypeTest scaffolds</name>
  <files>
    src/test/kotlin/riven/core/service/identity/IdentityInfrastructureIntegrationTest.kt,
    src/test/kotlin/riven/core/enums/integration/SourceTypeTest.kt
  </files>
  <action>
    **1. Create `src/test/kotlin/riven/core/service/identity/IdentityInfrastructureIntegrationTest.kt`**:

    This is a Testcontainers-based integration test using `@ActiveProfiles("integration")` and `@SpringBootTest`. Follow existing integration test patterns with `@DynamicPropertySource` for PostgreSQL container wiring. Use the shared Testcontainers base class if one exists, otherwise set up a PostgreSQL container.

    The test class should contain these test stubs (annotated with `@Test` and `@Disabled("Wave 0 scaffold — enable after 01-02 implementation")`):

    1. **`dedup index silently skips duplicate pending identity match job`** (INFRA-02) — insert a PENDING identity match queue entry, insert the same (workspace_id, entity_id, job_type) again, verify no duplicate exists (unique index causes silent skip or constraint violation).

    2. **`CHECK constraint rejects source_entity_id greater than target_entity_id`** (INFRA-06) — attempt to insert a match_suggestions row where source > target UUID, verify a constraint violation exception is thrown.

    3. **`pg_trgm GIN index exists on entity_attributes`** (INFRA-04) — query `pg_indexes` system catalog to verify `idx_entity_attributes_trgm` exists.

    4. **`unique pair constraint prevents duplicate active suggestions`** (INFRA-05) — insert two match_suggestions with same (workspace_id, source_entity_id, target_entity_id) and deleted=false, verify constraint violation.

    5. **`entity can belong to at most one identity cluster`** (INFRA-05) — insert an entity into two different clusters, verify unique constraint violation on identity_cluster_members.

    **2. Create `src/test/kotlin/riven/core/enums/integration/SourceTypeTest.kt`**:

    Simple unit test (no Spring context needed) with one test:

    1. **`IDENTITY_MATCH is a valid SourceType value`** (INFRA-03) — `assertDoesNotThrow { SourceType.valueOf("IDENTITY_MATCH") }` and verify `SourceType.entries.map { it.name }` contains "IDENTITY_MATCH". Mark with `@Disabled("Wave 0 scaffold — enable after 01-01 implementation")`.
  </action>
  <verify>
    <automated>cd /home/jared/dev/worktrees/identity-resolution/core && ./gradlew compileTestKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - IdentityInfrastructureIntegrationTest.kt exists with 5 disabled test stubs covering INFRA-02, INFRA-04, INFRA-05, INFRA-06
    - SourceTypeTest.kt exists with 1 disabled test stub covering INFRA-03
    - Both test files compile successfully
    - All tests are disabled so they do not fail before implementation
  </done>
</task>

</tasks>

<verification>
- `./gradlew compileTestKotlin` succeeds — all scaffolds compile
- Three test files exist at the paths specified in VALIDATION.md Wave 0 Requirements
- All test methods are `@Disabled` so the build remains green
</verification>

<success_criteria>
1. ExecutionQueueGenericizationTest.kt exists with 3 disabled regression test stubs
2. IdentityInfrastructureIntegrationTest.kt exists with 5 disabled integration test stubs
3. SourceTypeTest.kt exists with 1 disabled unit test stub
4. All test files compile without errors
5. `./gradlew test` still passes (disabled tests do not count as failures)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-00-SUMMARY.md`
</output>
