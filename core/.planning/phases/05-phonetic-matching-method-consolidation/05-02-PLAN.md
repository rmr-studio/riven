---
phase: 05-phonetic-matching-method-consolidation
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/test/kotlin/riven/core/service/identity/IdentityMatchPipelineIntegrationTest.kt
autonomous: true
requirements: [TEST-10, TEST-12]

must_haves:
  truths:
    - "Pipeline integration test confirms phonetic match (Smith/Smythe) produces a PENDING suggestion"
    - "Pipeline integration test confirms multi-strategy merge produces correct composite score with 2 signals (PHONETIC + EXACT_NORMALIZED)"
  artifacts:
    - path: "src/test/kotlin/riven/core/service/identity/IdentityMatchPipelineIntegrationTest.kt"
      provides: "TEST-10 and TEST-12 integration test methods"
      contains: "phonetic"
  key_links:
    - from: "IdentityMatchPipelineIntegrationTest"
      to: "fuzzystrmatch extension"
      via: "CREATE EXTENSION in BeforeAll"
      pattern: "fuzzystrmatch"
    - from: "TEST-10 test method"
      to: "findPhoneticCandidates"
      via: "end-to-end pipeline: trigger entity B -> candidate A via phonetic NAME match"
      pattern: "smythe.*smith|smith.*smythe"
    - from: "TEST-12 test method"
      to: "mergeCandidates + scoring"
      via: "end-to-end pipeline: 2 signals (PHONETIC NAME + EXACT_NORMALIZED PHONE) produce composite score"
      pattern: "PHONETIC.*EXACT_NORMALIZED|signals.*size.*2"
---

<objective>
Add pipeline integration tests TEST-10 (phonetic match produces suggestion) and TEST-12 (multi-strategy merge verifies signal provenance and composite scoring) to the existing IdentityMatchPipelineIntegrationTest.

Purpose: Validate the phonetic candidate query and multi-strategy merge work end-to-end against a real PostgreSQL instance with fuzzystrmatch installed.
Output: Two new integration test methods passing against Testcontainers PostgreSQL.
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-phonetic-matching-method-consolidation/05-CONTEXT.md
@.planning/phases/05-phonetic-matching-method-consolidation/05-RESEARCH.md
@.planning/phases/05-phonetic-matching-method-consolidation/05-01-SUMMARY.md

<interfaces>
<!-- Key integration test patterns from existing code -->

From src/test/kotlin/riven/core/service/identity/IdentityMatchPipelineIntegrationTest.kt:
- @ActiveProfiles("integration") with Testcontainers PostgreSQL
- BeforeAll: installExtensionsAndSeedData() — installs pg_trgm, inserts entity types, semantic metadata, entity attributes
- Helper methods: insertSemanticMetadata(), insertAttribute(), insertEntity() (or equivalent)
- Companion object: UUID constants for entity IDs, attribute IDs, attribute row IDs
- Test pattern: call identityResolutionService.resolveIdentity(triggerEntityId, workspaceId), assert suggestion status and signals

Existing companion object UUID ranges:
- TEST-07 (phone): 10000000-... range
- TEST-08 (nickname): 20000000-... range
- TEST-09/TEST-11 (email): 30000000-... range
- TEST-10/TEST-12 (phonetic): use 40000000-... range per RESEARCH.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fuzzystrmatch install + seed data for phonetic test entities</name>
  <files>
    src/test/kotlin/riven/core/service/identity/IdentityMatchPipelineIntegrationTest.kt
  </files>
  <behavior>
    - Verify: fuzzystrmatch extension installed in BeforeAll (dmetaphone() function exists and is callable)
    - Verify: Phonetic test entities seeded — entity A ("smythe" + phone "9995551234"), entity B ("smith" + phone "9995551234")
    - Verify: Semantic metadata wired for NAME and PHONE attributes with correct signal_type tags
  </behavior>
  <action>
**1. Add fuzzystrmatch extension install in `installExtensionsAndSeedData()`:**
Add `jdbcTemplate.execute("CREATE EXTENSION IF NOT EXISTS fuzzystrmatch")` alongside the existing `pg_trgm` install line.

**2. Add companion object UUID constants for TEST-10/TEST-12:**
Follow the 40000000-... range from RESEARCH.md:
```kotlin
// TEST-10 / TEST-12: Phonetic match + multi-strategy merge (Smythe/Smith + exact PHONE)
val attrNamePhoneticId = UUID.fromString("40000000-0000-0000-0000-000000000040")
val attrPhonePhoneticId = UUID.fromString("40000000-0000-0000-0000-000000000041")
val entityPhoneticA = UUID.fromString("30000000-0000-0000-0000-000000000040")
val entityPhoneticB = UUID.fromString("30000000-0000-0000-0000-000000000041")
val attrRowPhoneticAName = UUID.fromString("50000000-0000-0000-0000-000000000040")
val attrRowPhoneticAPhone = UUID.fromString("50000000-0000-0000-0000-000000000041")
val attrRowPhoneticBName = UUID.fromString("50000000-0000-0000-0000-000000000042")
val attrRowPhoneticBPhone = UUID.fromString("50000000-0000-0000-0000-000000000043")
```

**3. Seed test data in `installExtensionsAndSeedData()`:**
- Insert semantic metadata for attrNamePhoneticId (signal_type='NAME') and attrPhonePhoneticId (signal_type='PHONE')
- Insert entity rows for entityPhoneticA and entityPhoneticB
- Insert attributes:
  - entityPhoneticA: name="smythe", phone="9995551234"
  - entityPhoneticB: name="smith", phone="9995551234"
- Use the same phone number so EXACT_NORMALIZED PHONE strategy also matches (needed for TEST-12 multi-strategy merge)
- Phone "9995551234" must NOT overlap with any other test entity's phone number

Use the existing helper methods (insertSemanticMetadata, insertAttribute, etc.) following the established pattern from TEST-08 and TEST-09 seed blocks.
  </action>
  <verify>
    <automated>cd /home/jared/dev/worktrees/identity-resolution-enrichment/core && ./gradlew test --tests "riven.core.service.identity.IdentityMatchPipelineIntegrationTest" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - fuzzystrmatch installed in BeforeAll
    - Companion object has phonetic entity/attribute UUID constants
    - Two phonetic test entities seeded with NAME + PHONE attributes
    - All existing integration tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEST-10 phonetic match and TEST-12 multi-strategy merge integration tests</name>
  <files>
    src/test/kotlin/riven/core/service/identity/IdentityMatchPipelineIntegrationTest.kt
  </files>
  <behavior>
    - TEST-10: Triggering identity resolution on entityPhoneticB ("smith") finds entityPhoneticA ("smythe") via phonetic NAME match. Suggestion status is PENDING. At least one signal has matchSource=PHONETIC.
    - TEST-12: Same trigger as TEST-10 — the suggestion has exactly 2 signals (one PHONETIC for NAME, one EXACT_NORMALIZED for PHONE). Composite score is above MINIMUM_SCORE_THRESHOLD. The confidence gate passes (2 signals).
  </behavior>
  <action>
**TEST-10: Phonetic match produces suggestion (Smith/Smythe)**
- Call `identityResolutionService.resolveIdentity(entityPhoneticB, workspaceId)` (trigger "smith" finds "smythe")
- Assert: result suggestion list is not empty
- Assert: a suggestion exists where candidateEntityId == entityPhoneticA
- Assert: suggestion status is PENDING
- Assert: at least one signal in the suggestion has matchSource == MatchSource.PHONETIC
- Add KDoc: "TEST-10: Verifies that phonetically similar names (Smith/Smythe) produce a match suggestion via dmetaphone(). Entity B ('smith') triggers resolution and finds Entity A ('smythe') because both produce the same phonetic code 'SM0'."

**TEST-12: Multi-strategy merge verifies signal provenance**
- Same trigger: `identityResolutionService.resolveIdentity(entityPhoneticB, workspaceId)`
- Find the suggestion for entityPhoneticA
- Assert: suggestion has exactly 2 signals
- Assert: one signal has matchSource == MatchSource.PHONETIC (the NAME match)
- Assert: one signal has matchSource == MatchSource.EXACT_NORMALIZED (the PHONE match)
- Assert: suggestion composite score > MINIMUM_SCORE_THRESHOLD (import from scoring service companion object)
- Do NOT assert exact numeric score value
- Add KDoc: "TEST-12: Verifies multi-strategy merge produces correct composite scores. Entity B ('smith' + phone '9995551234') matches Entity A ('smythe' + same phone) via two strategies: phonetic NAME match (PHONETIC) and exact-digits PHONE match (EXACT_NORMALIZED). Asserts exactly 2 signals with correct provenance and composite score above threshold."

NOTE: TEST-10 and TEST-12 can be separate test methods or could be a single method that asserts both aspects. Per CONTEXT.md, they are described as related — use your judgment on grouping. Two separate methods is preferred for clarity.

Follow the assertion pattern of existing tests (TEST-07, TEST-08, TEST-09). Check what assertion library is used (likely JUnit 5 assertions or AssertJ) and match it.
  </action>
  <verify>
    <automated>cd /home/jared/dev/worktrees/identity-resolution-enrichment/core && ./gradlew test --tests "riven.core.service.identity.IdentityMatchPipelineIntegrationTest" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - TEST-10 passes: phonetic match Smith/Smythe produces PENDING suggestion with PHONETIC signal
    - TEST-12 passes: multi-strategy merge has exactly 2 signals (PHONETIC + EXACT_NORMALIZED) with composite score above threshold
    - All existing integration tests still pass
    - Full suite green: ./gradlew test
  </done>
</task>

</tasks>

<verification>
- `./gradlew test --tests "riven.core.service.identity.IdentityMatchPipelineIntegrationTest"` — all integration tests pass including TEST-10 and TEST-12
- `./gradlew test` — full suite green
- TEST-10: phonetic NAME match (Smith/Smythe) produces PENDING suggestion
- TEST-12: 2 signals (PHONETIC + EXACT_NORMALIZED), composite score above threshold
</verification>

<success_criteria>
- TEST-10 integration test passes: phonetically similar names produce match suggestion
- TEST-12 integration test passes: multi-strategy merge with correct signal provenance and composite score
- All existing integration tests unaffected
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-phonetic-matching-method-consolidation/05-02-SUMMARY.md`
</output>
