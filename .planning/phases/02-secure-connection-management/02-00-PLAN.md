---
phase: 02-secure-connection-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - core/src/test/kotlin/riven/core/service/util/factory/customsource/CustomSourceConnectionEntityFactory.kt
  - core/src/test/kotlin/riven/core/entity/customsource/CustomSourceConnectionEntityTest.kt
  - core/src/test/kotlin/riven/core/service/customsource/CredentialEncryptionServiceTest.kt
  - core/src/test/kotlin/riven/core/service/customsource/SsrfValidatorServiceTest.kt
  - core/src/test/kotlin/riven/core/service/customsource/ReadOnlyRoleVerifierServiceTest.kt
  - core/src/test/kotlin/riven/core/service/customsource/CustomSourceConnectionServiceTest.kt
  - core/src/test/kotlin/riven/core/controller/customsource/CustomSourceConnectionControllerTest.kt
  - core/src/test/kotlin/riven/core/configuration/customsource/LogRedactionTest.kt
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
  - CONN-04
  - CONN-05
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-05
  - SEC-06

must_haves:
  truths:
    - "All Phase 2 test files exist on disk as ignored/skipped skeletons"
    - "CustomSourceConnectionEntityFactory.kt exists per CLAUDE.md test factory rule"
    - "./gradlew test compiles with all new test files (they may @Disabled or have TODO placeholders)"
  artifacts:
    - path: "core/src/test/kotlin/riven/core/service/util/factory/customsource/CustomSourceConnectionEntityFactory.kt"
      provides: "Test factory for CustomSourceConnectionEntity"
      contains: "fun create"
    - path: "core/src/test/kotlin/riven/core/service/customsource/CredentialEncryptionServiceTest.kt"
      provides: "Placeholder test class for CONN-02"
    - path: "core/src/test/kotlin/riven/core/service/customsource/SsrfValidatorServiceTest.kt"
      provides: "Placeholder test class for SEC-01, SEC-02"
    - path: "core/src/test/kotlin/riven/core/service/customsource/ReadOnlyRoleVerifierServiceTest.kt"
      provides: "Placeholder test class for SEC-03"
    - path: "core/src/test/kotlin/riven/core/service/customsource/CustomSourceConnectionServiceTest.kt"
      provides: "Placeholder test class for CONN-03, SEC-05, SEC-06"
    - path: "core/src/test/kotlin/riven/core/entity/customsource/CustomSourceConnectionEntityTest.kt"
      provides: "Placeholder test class for CONN-01"
    - path: "core/src/test/kotlin/riven/core/controller/customsource/CustomSourceConnectionControllerTest.kt"
      provides: "Placeholder test class for CONN-05"
    - path: "core/src/test/kotlin/riven/core/configuration/customsource/LogRedactionTest.kt"
      provides: "Placeholder test class for CONN-04"
  key_links:
    - from: "CustomSourceConnectionEntityFactory"
      to: "CustomSourceConnectionEntity (Plan 01)"
      via: "imports + factory methods"
      pattern: "fun create.*CustomSourceConnectionEntity"
---

<objective>
Create Wave 0 test scaffolding so downstream implementation plans have concrete test files to populate without defining structure. Per VALIDATION.md, all Phase 2 MISSING test files must exist before Wave 1+ tasks begin. Per CLAUDE.md, `CustomSourceConnectionEntityFactory` must exist before any test constructs the entity.

Purpose: Unblock parallel Wave 1 execution. Each test file compiles (empty shell or @Disabled placeholders) so `./gradlew test` remains green as implementation lands.
Output: 8 empty/skeleton test files + 1 test factory, all compiling.
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-secure-connection-management/02-CONTEXT.md
@.planning/phases/02-secure-connection-management/02-VALIDATION.md
@.planning/phases/02-secure-connection-management/02-RESEARCH.md
@core/CLAUDE.md
@core/src/test/kotlin/riven/core/service/util/factory

<interfaces>
<!-- Test factory pattern reference (existing codebase convention) -->
<!-- Look at core/src/test/kotlin/riven/core/service/util/factory/integration/IntegrationConnectionEntityFactory.kt -->
<!-- if present; otherwise mirror other factories in service/util/factory/ -->

Test factory shape (CLAUDE.md rule — never construct JPA entities inline):
```kotlin
object CustomSourceConnectionEntityFactory {
    fun create(
        workspaceId: UUID = UUID.randomUUID(),
        name: String = "test-postgres",
        connectionStatus: ConnectionStatus = ConnectionStatus.CONNECTED,
        encryptedCredentials: ByteArray = ByteArray(32) { 0 },
        iv: ByteArray = ByteArray(12) { 0 },
        keyVersion: Int = 1,
    ): CustomSourceConnectionEntity = CustomSourceConnectionEntity(
        workspaceId = workspaceId,
        name = name,
        connectionStatus = connectionStatus,
        encryptedCredentials = encryptedCredentials,
        iv = iv,
        keyVersion = keyVersion,
    )
}
```
NOTE: CustomSourceConnectionEntity does not exist yet. Factory file references it — use `@Suppress("unused")` or a commented `import` placeholder + TODO until Plan 01 creates the entity, OR land this factory as a no-op class that Plan 01 updates. Preferred: land the factory as real code referencing the entity, and declare Plan 01 as dependency for gradle compilation. However since this is Wave 0 (no deps), use a `@file:Suppress(...)` file-level stub that Plan 01 fills in.

**Decision:** Create the factory file with a scoped TODO stub body (just a `// TODO(plan-01): populate` comment and an empty companion `object` so the file compiles). Plan 01's entity-creation task will finalize both entity and factory. This keeps Wave 0 strictly scaffolding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test factory stub + empty test class shells</name>
  <files>
    core/src/test/kotlin/riven/core/service/util/factory/customsource/CustomSourceConnectionEntityFactory.kt
    core/src/test/kotlin/riven/core/service/customsource/CredentialEncryptionServiceTest.kt
    core/src/test/kotlin/riven/core/service/customsource/SsrfValidatorServiceTest.kt
    core/src/test/kotlin/riven/core/service/customsource/ReadOnlyRoleVerifierServiceTest.kt
    core/src/test/kotlin/riven/core/service/customsource/CustomSourceConnectionServiceTest.kt
    core/src/test/kotlin/riven/core/entity/customsource/CustomSourceConnectionEntityTest.kt
    core/src/test/kotlin/riven/core/controller/customsource/CustomSourceConnectionControllerTest.kt
    core/src/test/kotlin/riven/core/configuration/customsource/LogRedactionTest.kt
  </files>
  <action>
    Create 8 Kotlin test files. Each test class file body is:

    ```kotlin
    package riven.core.<layer>.customsource

    import org.junit.jupiter.api.Disabled
    import org.junit.jupiter.api.Test

    /**
     * Phase 2 Wave 0 scaffold. Populated by plan 02-0N (see VALIDATION.md per-task map).
     * Covers: <REQ-IDS from VALIDATION.md row>
     */
    @Disabled("Phase 2 scaffold — populated by downstream plan")
    class <ClassName> {
        @Test fun placeholder() {}
    }
    ```

    For the factory file, create:

    ```kotlin
    package riven.core.service.util.factory.customsource

    /**
     * Test factory for CustomSourceConnectionEntity.
     * TODO(plan 02-01): populate with create() method once entity lands.
     * Required by CLAUDE.md: never construct JPA entities inline in tests.
     */
    object CustomSourceConnectionEntityFactory
    ```

    Why stubs: Wave 0 must compile without Plan 01's entity existing. @Disabled keeps the full suite green; placeholder() means the class compiles and is collected by JUnit 5 without asserting anything. Downstream plans remove @Disabled and fill in real test bodies.

    Name mapping per VALIDATION.md Wave 0 Requirements section:
      - CredentialEncryptionServiceTest — covers CONN-02
      - SsrfValidatorServiceTest — covers SEC-01, SEC-02
      - ReadOnlyRoleVerifierServiceTest — covers SEC-03
      - CustomSourceConnectionServiceTest — covers CONN-03, SEC-05, SEC-06
      - CustomSourceConnectionEntityTest — covers CONN-01
      - CustomSourceConnectionControllerTest — covers CONN-05
      - LogRedactionTest — covers CONN-04

    Do NOT reference any production classes (CustomSourceConnectionEntity, etc.) yet — those land in Plan 01.
  </action>
  <verify>
    <automated>cd core && ./gradlew compileTestKotlin</automated>
  </verify>
  <done>
    All 8 test files + 1 factory file exist on disk. `./gradlew compileTestKotlin` succeeds. Running `./gradlew test --tests "riven.core.*.customsource.*"` reports all tests skipped (@Disabled), 0 failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Wave 0 compilation + VALIDATION.md frontmatter update</name>
  <files>
    .planning/phases/02-secure-connection-management/02-VALIDATION.md
  </files>
  <action>
    Flip `wave_0_complete: false` → `true` in VALIDATION.md frontmatter. Set `nyquist_compliant: true` only if all downstream plans have <automated> on every task (this is validated at end of phase planning; leave as-is if not yet).

    Then run the quick suite to confirm scaffolding compiles and the Wave 0 files are on disk.
  </action>
  <verify>
    <automated>cd core && ./gradlew test --tests "riven.core.service.customsource.*" --tests "riven.core.entity.customsource.*" --tests "riven.core.controller.customsource.*" --tests "riven.core.configuration.customsource.*"</automated>
  </verify>
  <done>
    Frontmatter updated. Quick suite runs to completion; all customsource tests skipped (@Disabled) with 0 failures.
  </done>
</task>

</tasks>

<verification>
- 8 test files + 1 factory file exist
- `./gradlew compileTestKotlin` green
- `./gradlew test --tests "riven.core.*.customsource.*"` reports skipped, not failed
- VALIDATION.md `wave_0_complete: true`
</verification>

<success_criteria>
Downstream Wave 1 plans have pre-existing test files to populate. No plan needs to create a test file from scratch — they fill in placeholder() bodies and remove @Disabled.
</success_criteria>

<output>
Create `.planning/phases/02-secure-connection-management/02-00-SUMMARY.md` with files created, compile-time confirmation, and notes for downstream plans on the factory stub pattern.
</output>
