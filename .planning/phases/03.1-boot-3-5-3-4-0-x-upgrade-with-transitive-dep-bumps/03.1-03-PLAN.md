---
phase: 03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps
plan: 03
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - core/src/test/kotlin/**/*.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "No deprecated/removed Boot 3 test APIs remain in core/src/test"
    - "@MockBean / @SpyBean usages migrated to @MockitoBean / @MockitoSpyBean (Boot 4 canonical)"
    - "MockMvc and WebTestClient calls use Boot 4 fluent API (no deprecated overloads)"
    - "Hibernate 7 pgvector mapping has explicit test coverage exercising vector round-trip"
    - "Resilience4j direct-wiring (core + aop from Plan 01) has test coverage confirming circuit-breaker/retry annotations still activate"
  artifacts:
    - path: "core/src/test/kotlin/**/*.kt"
      provides: "Test harness on Boot 4 canonical APIs"
  key_links:
    - from: "Hibernate 7 runtime (Plan 01 bump)"
      to: "pgvector columns (NoteEntity, any embedding-typed columns)"
      via: "explicit integration test storing + reading a vector"
      pattern: "@Vector|VectorType|float\\[\\]"
    - from: "Resilience4j direct wiring (Plan 01)"
      to: "annotated service methods"
      via: "test that verifies @CircuitBreaker / @Retry annotations trigger"
      pattern: "@CircuitBreaker|@Retry"
---

<objective>
Sweep the test harness off Boot 3 idioms in one pass. Add coverage for the two runtime-critical zones created by Plan 01's dep bumps: (1) Hibernate 7 pgvector mapping, (2) Resilience4j direct-wiring replacing the starter. Runs in parallel with Plan 02 — different files, no conflict.

Purpose: Per CONTEXT.md, "sweep all tests off deprecated/removed Boot test APIs in one pass" (cleaner break than case-by-case). Runtime-critical Hibernate + Resilience4j changes get explicit coverage so Plan 04's closure gate can trust the full suite.
Output: Test sources compile + pass against Boot 4 test starter. New tests for vector round-trip + Resilience4j annotation activation.
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-CONTEXT.md
@.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-01-SUMMARY.md
@core/CLAUDE.md

<interfaces>
Boot 3 → Boot 4 test API changes (reference):

- `@MockBean` (org.springframework.boot.test.mock.mockito.MockBean) — DEPRECATED in 3.4+, REMOVED in 4.0. Replacement: `@MockitoBean` (org.springframework.test.context.bean.override.mockito.MockitoBean).
- `@SpyBean` → `@MockitoSpyBean` (same package).
- `MockMvcRequestBuilders` static helpers: most stable; watch for `post(...)` / `get(...)` overload signatures when combined with `.with(csrf())` + `.with(user(...))` — Security 7 may tighten.
- `WebTestClient.bindToController(...).build()` — verify no deprecated builder steps.
- `@AutoConfigureMockMvc` behaviour around security — Security 7 defaults may differ; explicit `.apply(springSecurity())` may be needed.
- `application-test.yml` uses H2 — Hibernate 7 H2 dialect may move; integration profile uses Testcontainers Postgres (preferred target for vector tests).

Current codebase state (from Grep):
- ZERO `@MockBean` occurrences in core/src/test → migration to @MockitoBean already happened OR the project uses a different mocking style. Verify by scanning broadly for deprecated symbols on first compile attempt.
- pgvector used in NoteEmbeddingService + NoteRepository + NoteEntity.
- Resilience4j annotations (`@CircuitBreaker`, `@Retry`) — grep usage to find target test surface.

Testing rules from core/CLAUDE.md (binding):
- Factory classes in `core/src/test/kotlin/riven/core/service/util/factory/` — NEVER construct JPA entities inline. Use/create factories.
- `@WithUserPersona` is mandatory for JWT-auth code paths. @Nested inner classes do NOT inherit it.
- Unit: H2 PG-compat. Integration: Testcontainers PostgreSQL with `@ActiveProfiles("integration")`.
- Prefer `whenever`/`verify` (mockito-kotlin) over `Mockito.when`/`Mockito.verify`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep deprecated/removed Boot test APIs from core/src/test</name>
  <files>core/src/test/kotlin/**/*.kt</files>
  <action>
Compile-first workflow: let the compiler tell you what broke, then fix in batches.

1. Run `./gradlew compileTestKotlin 2>&1 | tee /tmp/boot4-test-compile.log`. Triage the error output by symbol:

2. **@MockBean / @SpyBean** (if any surface — Grep found zero, but verify):
   ```
   grep -rn "org.springframework.boot.test.mock.mockito.MockBean" core/src/test
   grep -rn "org.springframework.boot.test.mock.mockito.SpyBean" core/src/test
   ```
   Replace import with `org.springframework.test.context.bean.override.mockito.MockitoBean` / `MockitoSpyBean` and rename the annotation accordingly. Sed-safe operation: imports and annotation names only — no semantic change.

3. **MockMvc API shifts:** if Boot 4 moved `MockMvcRequestBuilders` or tightened `.with(...)` signatures (Security 7 integration), update imports and call patterns. Typical breakage: `.with(SecurityMockMvcRequestPostProcessors.user(...))` — confirm still available; Security 7 docs will indicate replacement if any.

4. **WebTestClient:** update deprecated builder calls surfaced by compiler.

5. **Hibernate 7 test-side API surface:** any test that references `SessionFactory`, `EntityManager` low-level APIs, or `@Type(JsonBinaryType::class)` (if a test constructs entities needing jsonb mapping) — migrate per Plan 01's H7 patterns.

6. **Deprecated test starters / JUnit 4 leftovers:** Boot 4 drops JUnit 4 vintage engine from `spring-boot-starter-test`. If any `org.junit.Test` (vs `org.junit.jupiter.api.Test`) imports remain, migrate to JUnit 5.

7. After sweep, `./gradlew compileTestKotlin` must be BUILD SUCCESSFUL. Then `./gradlew test --tests '*'` — record delta between pass/fail counts and the HEAD baseline (Phase 3 end-state had 13 deferred failing tests per STATE.md; confirm none of the new failures are Boot 4 regressions vs pre-existing).

8. Do NOT "clean up" unrelated test smells opportunistically (CONTEXT.md: minimum-necessary policy). Out-of-scope drift (e.g. `Mockito.when` usage from Known Inconsistencies #4) stays untouched.

Factory-class rule (core/CLAUDE.md): if a test previously constructed an entity inline and that inline construction breaks under H7 type changes, move to / create a factory method — do NOT paper over with `@Suppress`.
  </action>
  <verify>
    <automated>cd core && ./gradlew compileTestKotlin 2>&1 | tail -10 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
  </verify>
  <done>`./gradlew compileTestKotlin` is BUILD SUCCESSFUL. Zero `@MockBean` / `@SpyBean` / deprecated MockMvc imports remain. Any newly-failing runtime tests documented in SUMMARY with classification (Boot 4 regression vs pre-existing deferred).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Hibernate 7 pgvector round-trip integration test</name>
  <files>core/src/test/kotlin/riven/core/entity/note/NoteEmbeddingHibernate7IntegrationTest.kt</files>
  <behavior>
    - Test 1: save a NoteEntity (or minimal vector-carrying entity) with a non-null embedding vector of expected dimension; reload via repository; assert returned vector == saved vector (element-wise)
    - Test 2: save an entity with null embedding; reload; assert embedding is null (nullability round-trip)
    - Test 3 (optional if cosine-distance query is used in prod): execute the production cosine-distance JPQL/native query against two persisted vectors and assert ordering matches expected similarity ranking
  </behavior>
  <action>
CONTEXT.md calls out pgvector mapping changes as "expected and must be covered by tests" under the Hibernate 7 bump. Add a focused integration test using the existing Testcontainers pgvector base class.

1. Create `core/src/test/kotlin/riven/core/entity/note/NoteEmbeddingHibernate7IntegrationTest.kt`:
   - Use `@ActiveProfiles("integration")` (Testcontainers pgvector).
   - Extend or inject the existing integration-test base class pattern (Phase 3 plans reference `ProjectionPipelineIntegrationTestConfig` and similar — match the established pattern).
   - Construct NoteEntity via the existing factory (or create a factory method if missing — per core/CLAUDE.md, never inline).
   - Persist + flush + clear + reload via NoteRepository. Assert the float[] vector contents survive the round-trip under Hibernate 7.
   - Dimension: use whatever ENRICHMENT_VECTOR_DIMENSIONS default the test profile expects (1536 for OpenAI; verify in application-integration.yml).

2. If Hibernate 7 introduces a new `@Vector` annotation or changes the VectorType coordinate, update NoteEntity in main sources accordingly (this is Plan 01 Task 2 territory — if it leaked here, fix in main sources and document in SUMMARY).

3. Run `./gradlew test --tests 'riven.core.entity.note.NoteEmbeddingHibernate7IntegrationTest'`. All 2-3 tests must pass against live Testcontainers pgvector.

This test is the Hibernate 7 canary — if pgvector storage regresses silently under H7, this fails Plan 04's closure gate.
  </action>
  <verify>
    <automated>cd core && ./gradlew test --tests 'riven.core.entity.note.NoteEmbeddingHibernate7IntegrationTest' 2>&1 | tail -10 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
  </verify>
  <done>NoteEmbeddingHibernate7IntegrationTest exists with 2-3 test methods covering save/reload round-trip + nullability. All tests pass under Hibernate 7 + hibernate-vector 7.x against Testcontainers pgvector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Resilience4j annotation-activation smoke test for direct-wiring</name>
  <files>core/src/test/kotlin/riven/core/configuration/resilience/Resilience4jDirectWiringTest.kt</files>
  <behavior>
    - Test 1: invoke a service method annotated `@CircuitBreaker(name="...")` and assert the circuit breaker registry records the invocation (via CircuitBreakerRegistry bean)
    - Test 2: repeatedly invoke a failing method and assert the breaker transitions to OPEN after configured failure threshold (exercises that direct-wiring actually intercepts)
  </behavior>
  <action>
Plan 01 dropped `resilience4j-spring-boot3:2.3.0` and wired `resilience4j-core` + `-aop` directly (CONTEXT.md laggard-dep decision). This must come with proof that `@CircuitBreaker` / `@Retry` annotations still activate — otherwise the prod runtime silently no-ops circuit breakers.

1. Identify an existing annotated service method:
   ```
   grep -rn "@CircuitBreaker\|@Retry" core/src/main --include="*.kt"
   ```
   Pick one real target (likely a Nango HTTP path or Supabase auth call).

2. Create `core/src/test/kotlin/riven/core/configuration/resilience/Resilience4jDirectWiringTest.kt`:
   - `@SpringBootTest` with minimal classes — the annotated service + `CircuitBreakerRegistry` bean + Spring AOP auto-config.
   - Inject `CircuitBreakerRegistry` and the service.
   - Call the service method via the proxy (not `new` — must route through AOP).
   - Assert `registry.circuitBreaker("<name>").metrics.numberOfBufferedCalls > 0` after invocation. If the AOP proxy isn't active, the registry stays empty → test fails → wiring regression caught.
   - Second assertion: force N consecutive failures (mock the underlying dep to throw), assert state transitions to OPEN.

3. If the annotated method is hard to exercise in isolation (external HTTP dep), create a minimal test-only `@Service` class inside the test file with `@CircuitBreaker(name = "testBreaker")` on a synthetic method, and configure `resilience4j.circuitbreaker.instances.testBreaker.*` in application-test.yml. Pure wiring test — does not prove the prod annotations per se, but proves the direct-wiring activates `@CircuitBreaker` AOP interception at all.

4. Run `./gradlew test --tests '*Resilience4jDirectWiring*'`.

Rationale: without this test, a silent AOP-wiring failure under Boot 4 + direct coordinates would only be caught in production when a flaky upstream dependency takes down the app.
  </action>
  <verify>
    <automated>cd core && ./gradlew test --tests '*Resilience4jDirectWiring*' 2>&1 | tail -10 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
  </verify>
  <done>Resilience4jDirectWiringTest exists with 2 tests proving @CircuitBreaker annotations are AOP-intercepted and state-transition semantics hold. Both tests pass. Confirms direct-wiring replacement for resilience4j-spring-boot3 is functional.</done>
</task>

</tasks>

<verification>
- `./gradlew compileTestKotlin` BUILD SUCCESSFUL.
- New tests from tasks 2 + 3 pass.
- Full-suite test-count delta documented: (passing before Plan 01) → (passing after Plan 03). Baseline: Phase 3 end-state with 13 deferred failures (STATE.md). Any NEW failures must be classified Boot 4 / H7 / Security 7 regressions.
</verification>

<success_criteria>
- Test harness compiles clean under Boot 4.
- pgvector round-trip proven under Hibernate 7.
- Resilience4j direct-wiring proven active.
- Newly-failing tests (if any) triaged in SUMMARY as regression-vs-pre-existing.
</success_criteria>

<output>
Create `.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-03-SUMMARY.md` with:
- Count + location of any deprecated test APIs migrated.
- Pre/post test-suite pass/fail counts with triage of any deltas.
- Confirmation that NoteEmbeddingHibernate7IntegrationTest + Resilience4jDirectWiringTest are green.
</output>
