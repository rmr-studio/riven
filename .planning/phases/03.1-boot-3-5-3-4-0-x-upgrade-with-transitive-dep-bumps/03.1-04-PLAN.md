---
phase: 03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps
plan: 04
type: execute
wave: 3
depends_on: [03.1-01, 03.1-02, 03.1-03]
files_modified:
  - core/build.gradle.kts
  - .planning/STATE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Full unit + integration test suite is green (./gradlew test BUILD SUCCESSFUL)"
    - "spring-boot-properties-migrator dependency is REMOVED from build.gradle.kts before merge"
    - "Manual Nango integration sync smoke has been executed end-to-end and confirmed working"
    - "No half-upgrade artifacts: no resilience4j-spring-boot3 coordinate, no vladmihalcea imports, no @MockBean imports, no Security 6 authorizeRequests DSL"
    - "STATE.md records phase 03.1 closure with final dep coordinates + Nango smoke evidence"
  artifacts:
    - path: "core/build.gradle.kts"
      provides: "Final Boot 4 state with properties-migrator removed"
      contains: "org.springframework.boot\" version \"4"
    - path: ".planning/STATE.md"
      provides: "Phase 03.1 closure record"
      contains: "Phase 03.1"
  key_links:
    - from: "Nango integration sync smoke (manual)"
      to: "IntegrationSyncWorkflowImpl (unchanged per PROJECT constraint)"
      via: "full connect → fetch → persist pathway exercised against a live Nango sandbox or recorded fixture"
      pattern: "IntegrationSyncWorkflowImpl"
---

<objective>
Closure gate. Run the full test suite to green. Execute a manual Nango integration sync smoke (PROJECT constraint: IntegrationSyncWorkflowImpl must keep working unchanged). Remove the properties-migrator dependency. Verify no half-upgrade artifacts remain. Record closure in STATE.md.

Purpose: Per CONTEXT.md Definition of Done, this plan is the sole arbiter of phase closure. If any gate fails, CONTEXT.md mandates branch abandonment — no flag-gated half-upgrade.
Output: Green test suite, Nango smoke evidence, clean build.gradle.kts, STATE.md closure entry.
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-CONTEXT.md
@.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-01-SUMMARY.md
@.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-02-SUMMARY.md
@.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-03-SUMMARY.md
@.planning/STATE.md
@core/build.gradle.kts

<interfaces>
Definition of Done verbatim from CONTEXT.md:

1. Full unit + integration test suite green.
2. Manual end-to-end smoke of existing Nango integration sync path.
3. Required smoke coverage: Nango integration sync path. Other zones (auth, Temporal workflows, JPA + pgvector) covered by test suite.
4. Rollback: branch-only. If blocked, abandon the branch. No flag-gated half-upgrade.
5. No perf baseline gate.

Half-upgrade tripwires (must all be zero):
- `grep -rn "vladmihalcea" core/src` → 0
- `grep -rn "resilience4j-spring-boot3" core/build.gradle.kts` → 0
- `grep -rn "authorizeRequests\s*{" core/src/main` → 0 (Security 6 DSL)
- `grep -rn "@MockBean\|@SpyBean" core/src/test` → 0 (Boot 3 mock APIs)
- `grep -rn "spring-boot-properties-migrator" core/build.gradle.kts` → 0 (must be removed at closure)

Nango smoke surface (PROJECT constraint in CONTEXT.md):
- IntegrationSyncWorkflowImpl is unchanged.
- Requires NANGO_SECRET_KEY + NANGO_BASE_URL env vars (from STACK.md).
- Either live sandbox (requires user-supplied keys) or recorded fixture — user decides at checkpoint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove properties-migrator, run final full test suite, scan for half-upgrade artifacts</name>
  <files>core/build.gradle.kts</files>
  <action>
1. Remove the properties-migrator dependency added by Plan 01:
   ```
   runtimeOnly("org.springframework.boot:spring-boot-properties-migrator")
   ```
   Delete this line from core/build.gradle.kts.

2. Run `./gradlew build test` with a clean cache: `./gradlew clean build test 2>&1 | tee /tmp/phase-03.1-final-build.log`. BUILD SUCCESSFUL is required. Any test failure blocks closure.

3. Cross-check against Phase 3 baseline (13 deferred failures per STATE.md): subtract those from the current failure count. Any NEW failure (that was green pre-Plan-01) must be fixed before closure — do NOT defer Boot 4 regressions into the Phase 3 deferred-items.md bucket.

4. Run the half-upgrade tripwire scan:
   ```bash
   echo "=== vladmihalcea imports ==="
   grep -rn "vladmihalcea" core/src || echo OK
   echo "=== resilience4j-spring-boot3 coordinate ==="
   grep -n "resilience4j-spring-boot3" core/build.gradle.kts || echo OK
   echo "=== Security 6 authorizeRequests DSL ==="
   grep -rn "authorizeRequests\s*{" core/src/main || echo OK
   echo "=== Boot 3 @MockBean / @SpyBean ==="
   grep -rn "@MockBean\|@SpyBean" core/src/test || echo OK
   echo "=== properties-migrator ==="
   grep -n "spring-boot-properties-migrator" core/build.gradle.kts || echo OK
   ```
   Every block must print `OK` (no grep match). Any hit = blocker.

5. If ALL tripwires pass and test suite is green, proceed to Task 2 (Nango smoke). Otherwise STOP — CONTEXT.md mandates branch abandonment over partial upgrade. Document the blocker in a new STATE.md blocker entry and halt the phase.
  </action>
  <verify>
    <automated>cd core && ./gradlew clean build test 2>&1 | tail -5 | grep -E "BUILD SUCCESSFUL|FAILED"</automated>
  </verify>
  <done>properties-migrator line removed from build.gradle.kts. `./gradlew clean build test` is BUILD SUCCESSFUL. All 5 tripwire greps report OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Nango integration sync smoke</name>
  <files>N/A (manual verification task — no file modifications)</files>
  <action>Execute manual Nango integration sync smoke per the how-to-verify steps below. Either Path A (live sandbox) or Path B (recorded fixture). Pause and wait for user approval signal before Task 3 executes.</action>
  <verify>User replies with approval signal per resume-signal block below.</verify>
  <done>User signs off with "approved live" or "approved recorded" + evidence (Temporal workflow ID or test class name). A "failed" response closes the phase as ABANDON.</done>
  <what-built>Boot 4 upgraded codebase with all deps on Boot 4 coordinates, Hibernate 7 runtime, Security 7 config, test suite green.</what-built>
  <how-to-verify>
Per CONTEXT.md, the Nango integration sync path is the canary. The PROJECT constraint is that `IntegrationSyncWorkflowImpl` must keep working unchanged. This checkpoint proves that end-to-end.

Pick ONE of the two verification paths:

**Path A (preferred — live sandbox):**
1. Export env vars:
   ```
   export NANGO_SECRET_KEY=<your sandbox key>
   export NANGO_BASE_URL=https://api.nango.dev
   ```
   Plus all other required vars from STACK.md (POSTGRES_DB_JDBC, JWT_*, SUPABASE_*, TEMPORAL_SERVER_ADDRESS, SERVER_PORT).
2. Start Temporal locally: `docker compose up temporal` (or your standard dev command).
3. Run the app: `./gradlew bootRun` — wait for "Started CoreApplication" log line with no Boot 4 property warnings in the output.
4. Create an integration via your standard Nango onboarding path (existing test workspace, Shopify or Hubspot sandbox). Trigger a sync (either via scheduled workflow waiting ≤15min, or manual POST if a manual-trigger endpoint exists on the integration path — not the custom-source path).
5. Observe Temporal UI (`http://localhost:8233`) — the IntegrationSyncWorkflow run must show Completed (green), no activity retries for framework-level errors (Boot/Hibernate/Security). Business-level errors (rate limit, auth) are OK; framework crashes are not.
6. Query the DB to confirm entities materialised: any `SELECT COUNT(*) FROM entities WHERE source_type = 'INTEGRATION' AND created_at > <smoke start>` returning > 0 confirms persistence path works under Boot 4 / Hibernate 7.

**Path B (recorded fixture, if no live sandbox available):**
Run the existing integration-sync integration test(s) — if the suite already covers the full IntegrationSyncWorkflowImpl pathway with mocked Nango HTTP responses, that counts. Confirm in the test output that IntegrationSyncActivitiesImpl + IntegrationSyncWorkflowImpl exercise ran green.

**Either path:** report below with evidence.
  </how-to-verify>
  <resume-signal>
Reply with ONE of:
- "approved live" + paste Temporal workflow ID + entity count delta
- "approved recorded" + paste the test class name(s) that exercised the path
- "failed: <description>" — phase closes as ABANDON per CONTEXT.md rollback clause
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record phase closure in STATE.md + update ROADMAP.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Only runs if Task 2 returned "approved" (live or recorded). If Task 2 returned "failed", abandon the branch per CONTEXT.md and do NOT execute this task.

1. **STATE.md** — append to Decisions section:
   ```
   - [Phase 03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps]: Plan 03.1-01: Final deps — Boot <version>, Hibernate <version>, hypersistence-utils-hibernate-70 <version>, hibernate-vector <version>, Temporal starter <version>, SpringDoc <version>, Resilience4j direct-wiring (core + aop, spring-boot3 coordinate removed), Spring Security unpinned (BOM-managed)
   - [Phase 03.1]: Plan 03.1-04 closure: Nango integration sync smoke PASSED via <live / recorded>, evidence: <workflow id / test class>. No Boot 4 regressions introduced; all tripwires OK.
   ```

   Update Current Position block:
   ```
   - **Phase:** 03.1 — Boot 4 upgrade COMPLETE. Phase 4 unblocked; 03-04 (MAP-07 Spring AI) unblocked pending rewrite against Spring AI 2.x.
   - **Status:** Complete
   ```

   Update Last Action / Next Action:
   ```
   ### Last Action
   Closed Phase 03.1. Codebase on Boot 4 + Hibernate 7 + Security 7. Test suite green. Nango smoke passed. Properties-migrator removed.

   ### Next Action
   Either (a) reopen 03-04 with Spring AI 2.x multi-provider rewrite, or (b) proceed to Phase 4 (IngestionOrchestrator + CustomSourceSyncWorkflow). User picks.
   ```

2. **ROADMAP.md** — Phase 03.1 section:
   - Change `**Goal:** [Urgent work - to be planned]` to a one-line real goal (e.g. "Spring Boot 3.5.3 → 4.0.x upgrade with Hibernate 7 + Security 7 migration, Nango integration sync proven unchanged.").
   - Check all 4 plan boxes: `- [x] 03.1-01-PLAN.md ...` through `- [x] 03.1-04-PLAN.md ...`.
   - Add a brief outcome line at the bottom of the Phase 03.1 block.

3. Commit with:
   ```
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(03.1): close phase 03.1 — Boot 4 upgrade complete" --files .planning/STATE.md .planning/ROADMAP.md
   ```
  </action>
  <verify>
    <automated>grep -q "Phase 03.1" .planning/STATE.md && grep -q "03.1-04-PLAN.md" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <done>STATE.md records phase 03.1 closure with final deps + Nango evidence. ROADMAP.md Phase 03.1 marked complete with real goal and all plans checked. Commit landed.</done>
</task>

</tasks>

<verification>
- `./gradlew clean build test` BUILD SUCCESSFUL with no Boot 4 regressions.
- 5 half-upgrade tripwire greps all report OK.
- Nango sync smoke signed off by user (live or recorded).
- STATE.md + ROADMAP.md updated and committed.
</verification>

<success_criteria>
- Phase 03.1 closes green OR branch is abandoned. No middle state.
- IntegrationSyncWorkflowImpl continues to work unchanged (PROJECT constraint preserved).
- All dep bumps final, no half-upgrade artifacts.
- Phase 4 + reopened Phase 03-04 both unblocked.
</success_criteria>

<output>
Create `.planning/phases/03.1-boot-3-5-3-4-0-x-upgrade-with-transitive-dep-bumps/03.1-04-SUMMARY.md` with:
- Final full-suite test pass/fail count vs Phase 3 baseline.
- Tripwire grep output (all OK).
- Nango smoke evidence: Temporal workflow ID + entity count, or recorded test class names.
- Final dep coordinate table.
- Closure decision: COMPLETE or ABANDON.
</output>
