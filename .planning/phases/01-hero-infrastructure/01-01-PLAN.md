---
phase: 01-hero-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/package.json
  - landing/package-lock.json
  - landing/app/globals.css
  - landing/lib/utils.ts
autonomous: true

must_haves:
  truths:
    - "All required npm packages are installed and resolvable"
    - "Design tokens (colors, typography) are defined in CSS"
    - "cn() utility is available for class composition"
  artifacts:
    - path: "landing/lib/utils.ts"
      provides: "cn() class merging utility"
      exports: ["cn"]
    - path: "landing/app/globals.css"
      provides: "Design tokens and theme variables"
      contains: "--color-primary"
  key_links:
    - from: "landing/lib/utils.ts"
      to: "clsx + tailwind-merge"
      via: "import"
      pattern: "twMerge.*clsx"
---

<objective>
Install all Phase 1 dependencies and establish the design system foundation.

Purpose: Create the technical and visual foundation that all subsequent plans depend on. Dependencies enable TanStack Query, form handling, and notifications. Design system establishes the "bold, not corporate" visual identity.

Output: node_modules populated with all libraries, globals.css with complete theme tokens, lib/utils.ts with cn() utility.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-hero-infrastructure/01-RESEARCH.md

@landing/package.json
@landing/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies matching /client versions</name>
  <files>landing/package.json, landing/package-lock.json</files>
  <action>
Run npm install in the landing directory for all Phase 1 dependencies:

```bash
cd /home/jared/dev/riven-landing-worktree/landing

# Core form + data fetching
npm install @tanstack/react-query@^5.81.2 react-hook-form@^7.58.1 zod@^3.25.67 @hookform/resolvers@^5.1.1

# Feedback
npm install sonner@^2.0.7

# UI utilities
npm install class-variance-authority@^0.7.1 clsx@^2.1.1 tailwind-merge@^3.3.1

# Icons
npm install lucide-react@^0.522.0

# Animation (for button states, future scroll effects)
npm install framer-motion@^12.23.24
```

Version pinning rationale: These versions match /client for consistency and are verified compatible with React 19 and Next.js 16.

Do NOT install any additional dependencies beyond those listed.
  </action>
  <verify>
Run `npm ls @tanstack/react-query react-hook-form zod sonner` to verify packages are installed.
Run `npm run build` to verify no type errors from new dependencies.
  </verify>
  <done>All 10 dependencies installed, package.json updated, npm run build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Establish design system and utilities</name>
  <files>landing/app/globals.css, landing/lib/utils.ts</files>
  <action>
**Create lib/utils.ts:**

Create `/home/jared/dev/riven-landing-worktree/landing/lib/utils.ts` with the cn() utility:

```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**Update globals.css:**

Replace the contents of `/home/jared/dev/riven-landing-worktree/landing/app/globals.css` with complete design system:

```css
@import "tailwindcss";

:root {
  /* Background and foreground */
  --background: hsl(0 0% 100%);
  --foreground: hsl(0 0% 3.9%);

  /* Primary - bold purple (anti-corporate) */
  --primary: hsl(262 83% 58%);
  --primary-foreground: hsl(0 0% 98%);

  /* Secondary */
  --secondary: hsl(0 0% 96.1%);
  --secondary-foreground: hsl(0 0% 9%);

  /* Muted for supporting text */
  --muted: hsl(0 0% 96.1%);
  --muted-foreground: hsl(0 0% 45.1%);

  /* Accent */
  --accent: hsl(0 0% 96.1%);
  --accent-foreground: hsl(0 0% 9%);

  /* Destructive for errors */
  --destructive: hsl(0 84.2% 60.2%);
  --destructive-foreground: hsl(0 0% 98%);

  /* Border, input, ring */
  --border: hsl(0 0% 89.8%);
  --input: hsl(0 0% 89.8%);
  --ring: hsl(262 83% 58%);

  /* Radius */
  --radius: 0.5rem;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: var(--radius);
  --radius-lg: calc(var(--radius) + 4px);

  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

body {
  background: var(--background);
  color: var(--foreground);
}
```

Design rationale:
- Primary purple (hsl 262 83% 58%) is bold and distinctive, not corporate blue
- Uses shadcn/ui naming conventions for future component compatibility
- Tailwind v4 @theme inline pattern for proper variable integration
- Removed dark mode for now (not in v1 scope)
  </action>
  <verify>
1. Verify lib/utils.ts exists: `ls landing/lib/utils.ts`
2. Verify globals.css has theme variables: `grep -c "primary" landing/app/globals.css` should return > 0
3. Run `npm run build` to verify CSS parses correctly
  </verify>
  <done>cn() utility exports correctly, globals.css has complete theme tokens with primary purple, build passes</done>
</task>

</tasks>

<verification>
1. `cd /home/jared/dev/riven-landing-worktree/landing && npm run build` - Should complete without errors
2. `npm ls @tanstack/react-query` - Should show version 5.x
3. `npm ls react-hook-form` - Should show version 7.x
4. `ls lib/utils.ts` - File exists
5. `grep "primary" app/globals.css` - Returns color definitions
</verification>

<success_criteria>
- All 10 dependencies from RESEARCH.md installed
- lib/utils.ts exports cn() function
- globals.css defines complete color palette with bold primary purple
- npm run build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-hero-infrastructure/01-01-SUMMARY.md`
</output>
