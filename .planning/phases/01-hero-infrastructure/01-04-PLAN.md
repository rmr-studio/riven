---
phase: 01-hero-infrastructure
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - landing/components/sections/hero.tsx
  - landing/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor sees bold headline within 5 seconds of page load"
    - "Visitor understands what Riven does from headline + subheadline"
    - "Visitor can enter email and submit with single click"
    - "Page uses consistent purple primary color"
    - "Product visual placeholder is visible"
  artifacts:
    - path: "landing/components/sections/hero.tsx"
      provides: "Hero section with headline, subheadline, form, and visual"
      exports: ["Hero"]
    - path: "landing/app/page.tsx"
      provides: "Landing page composing Hero section"
      contains: "Hero"
  key_links:
    - from: "landing/components/sections/hero.tsx"
      to: "landing/components/waitlist-form.tsx"
      via: "component import"
      pattern: "WaitlistForm"
    - from: "landing/app/page.tsx"
      to: "landing/components/sections/hero.tsx"
      via: "component import"
      pattern: "Hero"
---

<objective>
Build the Hero section with headline, subheadline, WaitlistForm, and product visual placeholder, then compose the landing page.

Purpose: This is what visitors see first. The hero must communicate value proposition immediately and make email capture frictionless. A visual element establishes product tangibility even pre-launch.

Output: Complete hero section with compelling copy, working email form, and placeholder for product visual. Landing page renders hero full-screen.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hero-infrastructure/01-RESEARCH.md

@landing/components/waitlist-form.tsx
@landing/components/ui/button.tsx
@landing/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero section component</name>
  <files>landing/components/sections/hero.tsx</files>
  <action>
Create `/home/jared/dev/riven-landing-worktree/landing/components/sections/hero.tsx`:

```typescript
import { WaitlistForm } from "@/components/waitlist-form";

export function Hero() {
  return (
    <section className="relative min-h-screen flex items-center">
      <div className="container mx-auto px-4 py-20 lg:py-32">
        <div className="grid gap-12 lg:grid-cols-2 lg:gap-16 items-center">
          {/* Left: Copy + Form */}
          <div className="flex flex-col gap-6 text-center lg:text-left">
            {/* Headline - HERO-01: 8 words or fewer, bold value prop */}
            <h1 className="text-4xl font-bold tracking-tight sm:text-5xl lg:text-6xl">
              Build a CRM that{" "}
              <span className="text-primary">fits your business</span>
            </h1>

            {/* Subheadline - HERO-02: Who it's for + problem solved */}
            <p className="text-lg text-muted-foreground sm:text-xl max-w-xl mx-auto lg:mx-0">
              Stop contorting your workflows to fit rigid tools. Riven adapts to
              how you actually work, not the other way around.
            </p>

            {/* Form - HERO-03 through HERO-07 */}
            <div className="mt-4">
              <WaitlistForm className="max-w-md mx-auto lg:mx-0" />
              <p className="mt-3 text-sm text-muted-foreground">
                Join the waitlist for early access. No spam, ever.
              </p>
            </div>
          </div>

          {/* Right: Product Visual - HERO-08 */}
          <div className="relative">
            {/* Placeholder for product mockup/visual */}
            <div className="aspect-[4/3] rounded-xl bg-gradient-to-br from-primary/20 via-primary/10 to-transparent border border-border flex items-center justify-center">
              <div className="text-center p-8">
                <div className="w-16 h-16 mx-auto mb-4 rounded-lg bg-primary/20 flex items-center justify-center">
                  <svg
                    className="w-8 h-8 text-primary"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      strokeLinecap="round"
                      strokeLinejoin="round"
                      strokeWidth={2}
                      d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z"
                    />
                  </svg>
                </div>
                <p className="text-sm text-muted-foreground">
                  Product preview coming soon
                </p>
              </div>
            </div>

            {/* Decorative gradient blur */}
            <div className="absolute -z-10 top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[120%] h-[120%] bg-primary/5 rounded-full blur-3xl" />
          </div>
        </div>
      </div>
    </section>
  );
}
```

Requirements addressed:
- HERO-01: "Build a CRM that fits your business" (7 words, bold value prop)
- HERO-02: Subheadline explains who (anyone frustrated with rigid tools) and what (adapts to your workflow)
- HERO-03 to HERO-07: WaitlistForm handles all form requirements
- HERO-08: Visual placeholder with gradient and icon establishes tangibility
- BRAND-04: Anti-enterprise feel through bold headline, casual copy, gradient accents

The product visual is a styled placeholder - ready to swap for actual mockup when available.
  </action>
  <verify>
1. `ls landing/components/sections/hero.tsx` - File exists
2. `grep "WaitlistForm" landing/components/sections/hero.tsx` - Form is included
3. `grep "text-primary" landing/components/sections/hero.tsx` - Uses primary color
4. `npm run build` - Build succeeds
  </verify>
  <done>Hero section has bold headline (7 words), descriptive subheadline, WaitlistForm, and product visual placeholder</done>
</task>

<task type="auto">
  <name>Task 2: Update landing page to render Hero</name>
  <files>landing/app/page.tsx</files>
  <action>
Replace contents of `/home/jared/dev/riven-landing-worktree/landing/app/page.tsx`:

```typescript
import { Hero } from "@/components/sections/hero";

export default function Home() {
  return (
    <main className="min-h-screen">
      <Hero />
    </main>
  );
}
```

Clean, minimal page that renders the hero section. No distractions - pure conversion focus.
  </action>
  <verify>
1. `grep "Hero" landing/app/page.tsx` - Hero is imported and used
2. `npm run build` - Build succeeds
3. `npm run dev` - Page loads without errors
  </verify>
  <done>Landing page renders Hero section as full-screen hero</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete hero section with:
- Bold headline: "Build a CRM that fits your business"
- Subheadline explaining the value proposition
- Email capture form with validation
- Loading/success/error states
- Product visual placeholder
- Purple primary color theme
  </what-built>
  <how-to-verify>
1. Start dev server: `cd /home/jared/dev/riven-landing-worktree/landing && npm run dev`
2. Open http://localhost:3000 in browser
3. Verify:
   - [ ] Headline is bold and readable within 5 seconds
   - [ ] Subheadline clearly explains what Riven does
   - [ ] Email input and "Join Waitlist" button are visible
   - [ ] Enter invalid email (like "test") -> blur field -> see validation error
   - [ ] Enter valid email -> click submit -> see loading spinner -> see success message
   - [ ] Primary purple color is visible in headline accent and button
   - [ ] Product visual placeholder is visible on right (on desktop) or below (on mobile)
4. Test error state: Stop the server, try submitting -> should see error toast
  </how-to-verify>
  <resume-signal>Type "approved" to confirm Phase 1 is complete, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` in landing directory - Should complete without errors
2. `npm run dev` - Page loads at http://localhost:3000
3. Visual inspection confirms hero layout and form functionality
4. Form validation works (reject invalid email)
5. Form submission works (loading -> success state)
</verification>

<success_criteria>
- HERO-01: Bold headline communicates value in 8 words or fewer
- HERO-02: Subheadline clarifies audience and problem
- HERO-03 to HERO-07: Form captures email with all states working
- HERO-08: Product visual placeholder is visible
- BRAND-01: Bold purple primary color throughout
- BRAND-02: Clear typography hierarchy (headline > subheadline > body)
- BRAND-04: Anti-enterprise aesthetic (not corporate blue, casual copy)
- Visitor can complete signup in under 10 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/01-hero-infrastructure/01-04-SUMMARY.md`
</output>
