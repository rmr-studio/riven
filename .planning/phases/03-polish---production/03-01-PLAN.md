---
phase: 03-polish---production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/components/sections/hero.tsx
  - landing/components/sections/pain-points.tsx
  - landing/components/sections/features.tsx
  - landing/components/sections/final-cta.tsx
  - landing/components/waitlist-form.tsx
  - landing/components/ui/button.tsx
  - landing/components/ui/input.tsx
autonomous: false

must_haves:
  truths:
    - "Hero CTA is visible without scrolling on mobile (320px-768px)"
    - "All form inputs and buttons are easily tappable on mobile (48px minimum)"
    - "Typography is readable at all breakpoints (mobile to desktop)"
    - "Features stack to single column on mobile"
    - "No horizontal scrolling occurs on mobile viewports"
  artifacts:
    - path: "landing/components/sections/hero.tsx"
      provides: "Mobile-first responsive layout with proper breakpoints"
      contains: "className.*md:|lg:"
    - path: "landing/components/ui/input.tsx"
      provides: "Touch-friendly input with 48px height"
      contains: "h-12|min-h-\\[48px\\]"
    - path: "landing/components/ui/button.tsx"
      provides: "Touch-friendly button with 48px minimum height"
      contains: "h-12|min-h-\\[48px\\]"
  key_links:
    - from: "All section components"
      to: "Responsive utilities"
      via: "Tailwind breakpoint prefixes (md:, lg:)"
      pattern: "className.*(?:md|lg):"
    - from: "Form inputs and buttons"
      to: "Touch target sizing"
      via: "h-12 or min-h-[48px] classes"
      pattern: "(?:h-12|min-h-\\[48px\\])"
---

<objective>
Implement mobile-first responsive design across all landing page sections, ensuring WCAG-compliant touch targets and readable typography at all breakpoints.

Purpose: Make the landing page usable and accessible on mobile devices (where most traffic originates), meeting RESP-01 through RESP-05 requirements.

Output: Fully responsive landing page with optimized mobile experience, proper touch targets, and progressive enhancement for larger screens.
</objective>

<execution_context>
@/home/jared/dev/riven-landing-worktree/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/dev/riven-landing-worktree/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jared/dev/riven-landing-worktree/.planning/PROJECT.md
@/home/jared/dev/riven-landing-worktree/.planning/ROADMAP.md
@/home/jared/dev/riven-landing-worktree/.planning/STATE.md
@/home/jared/dev/riven-landing-worktree/.planning/phases/03-polish---production/03-RESEARCH.md

# Current implementation
@/home/jared/dev/riven-landing-worktree/landing/components/sections/hero.tsx
@/home/jared/dev/riven-landing-worktree/landing/components/sections/pain-points.tsx
@/home/jared/dev/riven-landing-worktree/landing/components/sections/features.tsx
@/home/jared/dev/riven-landing-worktree/landing/components/sections/final-cta.tsx
@/home/jared/dev/riven-landing-worktree/landing/components/waitlist-form.tsx
@/home/jared/dev/riven-landing-worktree/landing/components/ui/button.tsx
@/home/jared/dev/riven-landing-worktree/landing/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Update UI primitives for mobile-first touch targets</name>
  <files>
    landing/components/ui/button.tsx
    landing/components/ui/input.tsx
  </files>
  <action>
Update Button and Input components to meet WCAG 2.5.8 touch target requirements (48px minimum).

**Button component (landing/components/ui/button.tsx):**
- Ensure `lg` variant has `h-12` (already exists based on STATE.md decision)
- Verify all interactive states (hover, focus-visible) work on mobile
- Keep existing variant system intact

**Input component (landing/components/ui/input.tsx):**
- Update default height to `h-12` (48px) for touch-friendly tapping
- Ensure `text-base` (16px) font size to prevent iOS auto-zoom on focus
- Add proper focus-visible ring for keyboard navigation
- Maintain existing border and background styling

Per RESEARCH.md Pattern 2: All form inputs must have 48px height and 16px minimum font size. Do NOT use smaller heights - touch targets are critical for mobile conversion.
  </action>
  <verify>
```bash
# Check Button has h-12
grep -n "h-12" landing/components/ui/button.tsx

# Check Input has h-12 and text-base
grep -n -E "(h-12|text-base)" landing/components/ui/input.tsx
```
  </verify>
  <done>
- Button lg variant includes h-12 class
- Input component has h-12 height and text-base font size
- Both components maintain existing functionality
  </done>
</task>

<task type="auto">
  <name>Implement mobile-first responsive layout for all sections</name>
  <files>
    landing/components/sections/hero.tsx
    landing/components/sections/pain-points.tsx
    landing/components/sections/features.tsx
    landing/components/sections/final-cta.tsx
  </files>
  <action>
Apply mobile-first responsive design to all section components using Tailwind's breakpoint system.

**Per RESEARCH.md Pattern 1 (Mobile-First):**
- Base styles (unprefixed) target mobile (320px+)
- md: prefix targets tablet (768px+)
- lg: prefix targets desktop (1024px+)

**Hero section (hero.tsx):**
- Section padding: `px-4 py-12 md:px-8 md:py-16 lg:px-12 lg:py-24`
- Container: `max-w-7xl mx-auto`
- Grid layout: `flex flex-col gap-8 md:flex-row md:items-center md:gap-12 lg:gap-16`
- Content column: `flex-1 space-y-6`
- Visual column: `flex-1 md:max-w-md lg:max-w-lg`
- Headline: `text-4xl font-bold md:text-5xl lg:text-6xl` (per STATE.md: 7-word headline)
- Subheadline: `text-lg text-muted-foreground md:text-xl lg:text-2xl`
- Form container: Ensure WaitlistForm is visible without scroll on mobile

**PainPoints section (pain-points.tsx):**
- Section padding: `px-4 py-12 md:px-8 md:py-16 lg:px-12 lg:py-24`
- Header: `text-3xl font-bold md:text-4xl lg:text-5xl`
- Grid: `grid grid-cols-1 gap-6 md:grid-cols-2 md:gap-8 lg:gap-10` (4 items in 2x2 grid on tablet+, stack on mobile)

**Features section (features.tsx):**
- Section padding: `px-4 py-12 md:px-8 md:py-16 lg:px-12 lg:py-24 bg-muted/30`
- Header: `text-3xl font-bold md:text-4xl lg:text-5xl`
- Grid: `grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4 lg:gap-8` (RESP-04: single column on mobile)

**FinalCTA section (final-cta.tsx):**
- Section padding: `px-4 py-12 md:px-8 md:py-16 lg:px-12 lg:py-24 bg-muted/50`
- Container: `max-w-2xl mx-auto text-center` (centered layout per STATE.md)
- Headline: `text-3xl font-bold md:text-4xl lg:text-5xl`

**Critical:** Ensure all text is at least 16px on mobile for readability. Per RESEARCH.md Pitfall 7, responsive typography is essential.
  </action>
  <verify>
```bash
# Start dev server in background
cd /home/jared/dev/riven-landing-worktree/landing && npm run dev &

# Wait for server to start
sleep 5

# Verify responsive classes are present in each section
grep -n "md:" landing/components/sections/hero.tsx | head -5
grep -n "grid-cols-1" landing/components/sections/features.tsx
grep -n "px-4.*py-12" landing/components/sections/pain-points.tsx

# Check Hero has proper text scaling
grep -n "text-4xl.*md:text-5xl.*lg:text-6xl" landing/components/sections/hero.tsx
```
  </verify>
  <done>
- All sections have mobile-first padding (px-4 py-12 base, scaling up with md/lg)
- Hero headline scales from text-4xl to text-6xl across breakpoints
- Features grid stacks to single column on mobile (grid-cols-1)
- Typography is readable at all breakpoints (minimum 16px)
- No hardcoded pixel widths that break mobile layout
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Mobile-first responsive design across all landing page sections with WCAG-compliant touch targets
  </what-built>
  <how-to-verify>
1. Open dev server at http://localhost:3000

2. **Mobile verification (320px - 480px):**
   - Open Chrome DevTools (F12)
   - Toggle device toolbar (Cmd/Ctrl + Shift + M)
   - Select "iPhone SE" (375×667) or "Galaxy S8+" (360×740)
   - Expected: Hero CTA visible without scrolling (RESP-02)
   - Expected: All buttons/inputs easily tappable (no mis-taps)
   - Expected: No horizontal scrolling
   - Expected: Text is readable (not too small)

3. **Form touch targets:**
   - Tap email input field - should be easy to hit
   - Tap submit button - should be easy to hit
   - Expected: Both have comfortable touch areas (48px height)

4. **Typography readability:**
   - Read hero headline - should be large and clear
   - Read pain points - should be readable
   - Expected: No text smaller than 16px

5. **Tablet verification (768px):**
   - Switch to "iPad" (768×1024)
   - Expected: Features grid shows 2 columns
   - Expected: Hero layout switches to 2-column (copy left, visual right)
   - Expected: Comfortable spacing and readable text

6. **Desktop verification (1024px+):**
   - Switch to "Laptop with HiDPI screen" (1440×900)
   - Expected: Features grid shows 4 columns
   - Expected: Proper spacing and hierarchy
   - Expected: Hero uses full width effectively

7. **Features single-column check (RESP-04):**
   - Back to "iPhone SE"
   - Scroll to Features section
   - Expected: Feature cards stack vertically (1 card width)
   - Expected: Cards are readable and well-spaced

**Issues to report:**
- Any horizontal scrolling on mobile
- Any buttons/inputs difficult to tap
- Any text too small to read comfortably
- Any layout breaking or overlapping
- Hero CTA not visible without scrolling on mobile
  </how-to-verify>
  <resume-signal>
Type "approved" if all responsive behaviors work correctly, or describe specific issues that need fixing
  </resume-signal>
</task>

</tasks>

<verification>
1. Dev server running at localhost:3000
2. All section files contain responsive Tailwind classes (md:, lg: prefixes)
3. Button and Input components have 48px minimum height
4. Mobile viewport (320px-768px): Hero CTA visible without scroll, no horizontal scroll
5. Features grid: 1 column on mobile, 2 on tablet, 4 on desktop
6. All interactive elements easily tappable on mobile
7. Typography scales appropriately (text-4xl → text-6xl for headlines)
</verification>

<success_criteria>
1. RESP-01 fulfilled: Mobile-first design approach with unprefixed base styles
2. RESP-02 fulfilled: Hero CTA visible without scrolling on mobile (320px-768px)
3. RESP-03 fulfilled: All form inputs and buttons have 48px minimum touch targets
4. RESP-04 fulfilled: Features stack to single column on mobile
5. RESP-05 fulfilled: Typography scales across breakpoints (4xl → 5xl → 6xl for headlines)
6. No horizontal scrolling on any viewport size
7. All text meets 16px minimum for readability
8. Human verification confirms smooth responsive experience
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish---production/03-01-SUMMARY.md`
</output>
