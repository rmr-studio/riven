---
phase: 03-polish---production
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/components/footer.tsx
  - landing/app/page.tsx
  - landing/public/logo.svg
autonomous: false

must_haves:
  truths:
    - "Footer displays logo/wordmark"
    - "Footer shows copyright notice with current year"
    - "Footer provides contact email link"
    - "Logo appears in footer with proper sizing and spacing"
  artifacts:
    - path: "landing/components/footer.tsx"
      provides: "Footer component with logo, contact link, copyright"
      min_lines: 30
      exports: ["Footer"]
    - path: "landing/public/logo.svg"
      provides: "Logo SVG asset"
      contains: "<svg"
    - path: "landing/app/page.tsx"
      provides: "Page with Footer integrated"
      contains: "Footer"
  key_links:
    - from: "landing/components/footer.tsx"
      to: "landing/public/logo.svg"
      via: "Next.js Image component"
      pattern: "Image.*src.*logo\\.svg"
    - from: "landing/app/page.tsx"
      to: "Footer component"
      via: "Import and render"
      pattern: "import.*Footer.*from.*footer|<Footer"
---

<objective>
Create production-ready footer component with branding, contact information, and copyright notice, fulfilling BRAND-03 and FOOT-01 through FOOT-03 requirements.

Purpose: Complete the landing page with essential footer elements, establish brand presence through logo/wordmark, and provide contact mechanism.

Output: Responsive footer component integrated into landing page with logo asset, contact link, and dynamic copyright year.
</objective>

<execution_context>
@/home/jared/dev/riven-landing-worktree/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/dev/riven-landing-worktree/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jared/dev/riven-landing-worktree/.planning/PROJECT.md
@/home/jared/dev/riven-landing-worktree/.planning/ROADMAP.md
@/home/jared/dev/riven-landing-worktree/.planning/STATE.md
@/home/jared/dev/riven-landing-worktree/.planning/phases/03-polish---production/03-RESEARCH.md

# Current page structure
@/home/jared/dev/riven-landing-worktree/landing/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Create placeholder logo SVG</name>
  <files>
    landing/public/logo.svg
  </files>
  <action>
Create a simple, bold wordmark SVG for "Riven" using the established brand color (primary purple from STATE.md: 262 83% 58%).

**SVG specifications:**
- Size: 160×40px (standard logo dimensions)
- Content: "RIVEN" text wordmark
- Font weight: Bold, sans-serif style
- Color: Use HSL(262, 83%, 58%) - the established brand primary color
- Clean, geometric letterforms reflecting anti-enterprise brand (BRAND-04)

**Technical:**
```xml
<svg width="160" height="40" viewBox="0 0 160 40" xmlns="http://www.w3.org/2000/svg">
  <text x="0" y="30" font-family="system-ui, -apple-system, sans-serif" font-size="28" font-weight="700" fill="hsl(262, 83%, 58%)">RIVEN</text>
</svg>
```

This is a placeholder wordmark. The user can replace with their actual logo later without code changes - just swap the file at public/logo.svg.

Per RESEARCH.md Pattern 3: SVG format is optimal for logos (scalability, Next.js Image handles optimization).
  </action>
  <verify>
```bash
# Check logo file exists and is valid SVG
test -f landing/public/logo.svg && echo "Logo exists"
grep -q "<svg" landing/public/logo.svg && echo "Valid SVG"
grep -q "RIVEN" landing/public/logo.svg && echo "Contains wordmark"
```
  </verify>
  <done>
- landing/public/logo.svg exists
- SVG contains "RIVEN" wordmark
- Uses brand purple color
- 160×40px dimensions suitable for header/footer
  </done>
</task>

<task type="auto">
  <name>Create Footer component</name>
  <files>
    landing/components/footer.tsx
  </files>
  <action>
Create Footer component following RESEARCH.md Pattern 5 (Footer Component Structure) with mobile-first responsive design.

**Footer component structure:**

```tsx
import Image from "next/image"

export function Footer() {
  const currentYear = new Date().getFullYear()

  return (
    <footer className="
      border-t
      bg-background
      px-4 py-8
      md:px-8 md:py-12
      lg:px-12
    ">
      <div className="
        mx-auto
        max-w-7xl
        flex flex-col
        gap-6
        md:flex-row
        md:items-center
        md:justify-between
      ">
        {/* FOOT-03: Logo/wordmark */}
        <div className="flex-shrink-0">
          <Image
            src="/logo.svg"
            alt="Riven"
            width={160}
            height={40}
            className="h-8 w-auto"
          />
        </div>

        {/* FOOT-02: Contact email link */}
        <nav className="flex gap-6">
          <a
            href="mailto:hello@riven.dev"
            className="
              text-sm text-muted-foreground
              hover:text-foreground
              transition-colors
              min-h-[48px]
              flex items-center
            "
          >
            Contact
          </a>
        </nav>

        {/* FOOT-01: Copyright notice */}
        <p className="text-sm text-muted-foreground">
          © {currentYear} Riven. All rights reserved.
        </p>
      </div>
    </footer>
  )
}
```

**Key implementation details:**
- FOOT-01: Dynamic copyright year using `new Date().getFullYear()` (per RESEARCH.md anti-pattern: don't hardcode year)
- FOOT-02: Contact email link with 48px min-height for touch targets
- FOOT-03: Logo via Next.js Image component for optimization
- Mobile-first: Stack vertically on mobile, horizontal layout on tablet+
- Semantic HTML: `<footer>` element with `<nav>` for links
- Border-top to separate from page content
- Uses design tokens (border, bg-background, text-muted-foreground)

Per RESEARCH.md: Contact link must have min-h-[48px] for touch targets. Logo uses Next.js Image for automatic optimization.
  </action>
  <verify>
```bash
# Check Footer component exists and exports properly
grep -n "export function Footer" landing/components/footer.tsx

# Verify Footer includes all required elements
grep -n "new Date().getFullYear()" landing/components/footer.tsx  # FOOT-01
grep -n "mailto:" landing/components/footer.tsx                     # FOOT-02
grep -n "logo.svg" landing/components/footer.tsx                    # FOOT-03

# Check touch target on contact link
grep -n "min-h-\\[48px\\]" landing/components/footer.tsx
```
  </verify>
  <done>
- Footer component created with named export
- FOOT-01: Copyright notice with dynamic year
- FOOT-02: Contact email link with proper touch target
- FOOT-03: Logo/wordmark using Next.js Image
- Mobile-first responsive layout (flex-col → flex-row)
- Semantic HTML structure
  </done>
</task>

<task type="auto">
  <name>Integrate Footer into landing page</name>
  <files>
    landing/app/page.tsx
  </files>
  <action>
Update landing page to include Footer component after all sections.

**Add Footer import:**
```tsx
import { Footer } from "@/components/footer";
```

**Update page structure:**
```tsx
export default function Home() {
  return (
    <main className="min-h-screen">
      <Hero />
      <PainPoints />
      <Features />
      <FinalCTA />
      <Footer />
    </main>
  );
}
```

Simple integration - Footer as final element after FinalCTA section. No layout changes needed since Footer is self-contained with its own spacing.
  </action>
  <verify>
```bash
# Check Footer is imported
grep -n 'import.*Footer.*from.*footer' landing/app/page.tsx

# Check Footer is rendered
grep -n '<Footer />' landing/app/page.tsx

# Verify order (Footer after FinalCTA)
grep -n -A 2 'FinalCTA' landing/app/page.tsx | grep Footer
```
  </verify>
  <done>
- Footer component imported in page.tsx
- Footer rendered as final element in main
- Footer appears after FinalCTA section
- Page structure: Hero → PainPoints → Features → FinalCTA → Footer
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Footer component with logo, contact link, and copyright notice integrated into landing page
  </what-built>
  <how-to-verify>
1. Open dev server at http://localhost:3000 (if not already running: `cd landing && npm run dev`)

2. **Scroll to bottom of page:**
   - Expected: Footer appears after Final CTA section
   - Expected: Visual separation (border-top line)

3. **Footer content verification:**
   - Expected: "RIVEN" logo/wordmark visible on left (mobile) or left side (desktop)
   - Expected: "Contact" link visible
   - Expected: Copyright text "© 2026 Riven. All rights reserved." visible
   - Expected: All three elements present and readable

4. **Mobile layout (375px):**
   - Open DevTools, switch to iPhone SE
   - Scroll to footer
   - Expected: Logo, Contact link, and Copyright stack vertically
   - Expected: Proper spacing between elements
   - Expected: Contact link is easily tappable (48px height)

5. **Desktop layout (1440px):**
   - Switch to desktop viewport
   - Expected: Logo on left, Contact link in middle, Copyright on right (horizontal layout)
   - Expected: Elements aligned properly
   - Expected: Comfortable spacing

6. **Interaction test:**
   - Hover over "Contact" link
   - Expected: Text color changes (hover state works)
   - Click "Contact" link
   - Expected: Opens email client with "hello@riven.dev"

7. **Logo rendering:**
   - Expected: Logo is crisp and clear (SVG, not pixelated)
   - Expected: Logo uses purple brand color
   - Expected: Logo scales appropriately

**Issues to report:**
- Missing footer elements (logo, contact, copyright)
- Layout issues (overlapping, misalignment)
- Contact link not working or hard to tap
- Logo not visible or rendering incorrectly
- Incorrect copyright year
- Poor spacing or typography
  </how-to-verify>
  <resume-signal>
Type "approved" if footer displays correctly with all elements, or describe issues that need fixing. If logo needs to be replaced with actual brand logo, note that but approve the placeholder implementation.
  </resume-signal>
</task>

</tasks>

<verification>
1. landing/public/logo.svg exists with RIVEN wordmark
2. landing/components/footer.tsx created with Footer export
3. Footer includes logo (Image component), contact link (mailto:), copyright (dynamic year)
4. landing/app/page.tsx imports and renders Footer
5. Footer appears after FinalCTA in page structure
6. Footer responsive layout works (stack on mobile, horizontal on desktop)
7. Contact link has 48px touch target
8. Human verification confirms footer displays correctly
</verification>

<success_criteria>
1. BRAND-03 fulfilled: Logo/wordmark present in footer
2. FOOT-01 fulfilled: Copyright notice with dynamic year
3. FOOT-02 fulfilled: Contact email link (hello@riven.dev)
4. FOOT-03 fulfilled: Logo/wordmark in footer using Next.js Image
5. Footer is responsive (vertical stack on mobile, horizontal on desktop)
6. All footer links meet 48px touch target requirement
7. Semantic HTML structure with proper footer and nav elements
8. Human verification confirms all footer elements work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish---production/03-02-SUMMARY.md`
</output>
