---
phase: 01-adapter-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/main/kotlin/riven/core/enums/integration/SourceType.kt
  - core/src/main/kotlin/riven/core/models/ingestion/adapter/RecordBatch.kt
  - core/src/main/kotlin/riven/core/models/ingestion/adapter/SyncMode.kt
  - core/src/main/kotlin/riven/core/models/ingestion/adapter/SourceRecord.kt
  - core/src/main/kotlin/riven/core/models/ingestion/adapter/SchemaIntrospectionResult.kt
  - core/src/test/kotlin/riven/core/models/ingestion/adapter/RecordBatchTest.kt
  - core/src/test/kotlin/riven/core/models/ingestion/adapter/SyncModeTest.kt
  - core/src/test/kotlin/riven/core/entity/entity/SourceTypeJpaRoundTripTest.kt
autonomous: true
requirements: [ADPT-02, ADPT-03, ADPT-04]
must_haves:
  truths:
    - "Any Kotlin service can instantiate RecordBatch(records, nextCursor, hasMore) and read its three fields"
    - "SyncMode has exactly four values: POLL, CDC, PUSH, ONE_SHOT"
    - "SourceType.CUSTOM_SOURCE exists and round-trips through JPA on EntityTypeEntity.sourceType"
    - "SourceRecord carries neutral externalId + payload + optional sourceMetadata (future-proof across Nango/Postgres/CSV)"
    - "SchemaIntrospectionResult carries tables + columns + types + nullable (minimal shape extensible by Phase 3)"
  artifacts:
    - path: "core/src/main/kotlin/riven/core/models/ingestion/adapter/RecordBatch.kt"
      provides: "RecordBatch data class"
      contains: "data class RecordBatch"
    - path: "core/src/main/kotlin/riven/core/models/ingestion/adapter/SyncMode.kt"
      provides: "SyncMode enum"
      contains: "enum class SyncMode"
    - path: "core/src/main/kotlin/riven/core/models/ingestion/adapter/SourceRecord.kt"
      provides: "SourceRecord neutral DTO for adapter records"
      contains: "data class SourceRecord"
    - path: "core/src/main/kotlin/riven/core/models/ingestion/adapter/SchemaIntrospectionResult.kt"
      provides: "SchemaIntrospectionResult + TableSchema + ColumnSchema"
      contains: "data class SchemaIntrospectionResult"
    - path: "core/src/main/kotlin/riven/core/enums/integration/SourceType.kt"
      provides: "SourceType enum with CUSTOM_SOURCE value"
      contains: "CUSTOM_SOURCE"
  key_links:
    - from: "core/src/main/kotlin/riven/core/enums/integration/SourceType.kt"
      to: "EntityTypeEntity.sourceType"
      via: "@Enumerated(EnumType.STRING)"
      pattern: "CUSTOM_SOURCE"
    - from: "core/src/test/kotlin/riven/core/entity/entity/SourceTypeJpaRoundTripTest.kt"
      to: "SourceType.CUSTOM_SOURCE"
      via: "JPA save + read via H2"
      pattern: "CUSTOM_SOURCE"
---

<objective>
Establish the neutral contract data types (RecordBatch, SyncMode, SourceRecord, SchemaIntrospectionResult) and extend SourceType with CUSTOM_SOURCE. This is the foundation layer — nothing else in Phase 1 can compile without these types.

Purpose: Interface + NangoAdapter (downstream plans) must import these types. Phase 3 PostgresAdapter and Phase 4 Orchestrator will consume SourceRecord/RecordBatch/SchemaIntrospectionResult unchanged.
Output: Five new Kotlin files in `models/ingestion/adapter/`, one enum value added, three Wave 0 test files.
</objective>

<execution_context>
@/home/jared/.claude/get-shit-done/workflows/execute-plan.md
@/home/jared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-adapter-foundation/01-CONTEXT.md
@.planning/phases/01-adapter-foundation/01-RESEARCH.md
@.planning/phases/01-adapter-foundation/01-VALIDATION.md
@core/CLAUDE.md

<interfaces>
<!-- Existing codebase contracts this plan builds on -->

From core/src/main/kotlin/riven/core/enums/integration/SourceType.kt (BEFORE change — verify with read first):
```kotlin
enum class SourceType {
    USER_CREATED,
    INTEGRATION,
    IMPORT,
    API,
    WORKFLOW,
    IDENTITY_MATCH,
    TEMPLATE,
    PROJECTED,
    // CUSTOM_SOURCE to be ADDED
}
```

From core/src/main/kotlin/riven/core/entity/entity/EntityTypeEntity.kt (persistence pattern — DO NOT modify):
```kotlin
@Enumerated(EnumType.STRING)
@Column(name = "source_type", length = 50)
var sourceType: SourceType = SourceType.USER_CREATED
```

Existing siblings in `models/ingestion/` (pattern reference, DO NOT modify):
- `ProjectionResult.kt` — pure Kotlin data class, no JPA
- `ResolutionResult.kt` — same shape convention
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write failing tests for data types + enum round-trip</name>
  <files>
    core/src/test/kotlin/riven/core/models/ingestion/adapter/RecordBatchTest.kt,
    core/src/test/kotlin/riven/core/models/ingestion/adapter/SyncModeTest.kt,
    core/src/test/kotlin/riven/core/entity/entity/SourceTypeJpaRoundTripTest.kt
  </files>
  <behavior>
    - RecordBatchTest: `RecordBatch(records=emptyList(), nextCursor=null, hasMore=false)` constructs; destructuring returns the three fields unchanged; `nextCursor: String?` is nullable.
    - RecordBatchTest: `RecordBatch(records=listOf(SourceRecord("ext-1", mapOf("a" to 1), null)), nextCursor="t-42", hasMore=true)` preserves values.
    - SyncModeTest: `SyncMode.values()` returns exactly `[POLL, CDC, PUSH, ONE_SHOT]` in declaration order (assert `size == 4` and names equal set).
    - SourceTypeJpaRoundTripTest: using existing test infrastructure (H2, `ddl-auto: create-drop`, `@SpringBootTest` or existing `EntityTypeRepositoryTest` style), save an `EntityTypeEntity` with `sourceType = SourceType.CUSTOM_SOURCE`, flush, clear, reload by id, and assert `sourceType == SourceType.CUSTOM_SOURCE`. Use `IntegrationTestFactory` or whatever entity factory exists per core/CLAUDE.md (never construct entities inline — grep `core/src/test/kotlin/**/factory/` for existing EntityTypeEntity factory; if none, add a minimal one under `service/util/factory/`).
  </behavior>
  <action>
    Create three test files. DO NOT create implementation yet — tests MUST fail compile (RecordBatch, SourceRecord, SyncMode.ONE_SHOT don't exist) or fail at runtime (CUSTOM_SOURCE enum value missing).

    Steps:
    1. Read `core/src/test/kotlin/riven/core/entity/entity/EntityTypeRepositoryTest.kt` if it exists to mirror its JPA test style (profile, annotations, repository injection).
    2. Check `core/src/test/kotlin/riven/core/service/util/factory/` for an existing EntityTypeEntity factory. If none, skip inline construction — instead write a minimal `EntityTypeTestFactory.create(sourceType: SourceType): EntityTypeEntity` helper in `service/util/factory/EntityTypeTestFactory.kt` per core/CLAUDE.md factory rule (but keep it out of `files_modified` if extending an existing file).
    3. Write RecordBatchTest using JUnit 5 + AssertJ (`assertThat`).
    4. Write SyncModeTest — pure enum assertion, no Spring.
    5. Write SourceTypeJpaRoundTripTest using `@DataJpaTest` or `@SpringBootTest` matching project convention. Must use H2 profile (`application-test.yml`).

    Do NOT use `!!` on nullable IDs — use `requireNotNull(saved.id) { "id should be assigned after flush" }` per project memory.
  </action>
  <verify>
    <automated>cd core && ./gradlew test --tests "riven.core.models.ingestion.adapter.RecordBatchTest" --tests "riven.core.models.ingestion.adapter.SyncModeTest" --tests "riven.core.entity.entity.SourceTypeJpaRoundTripTest" 2>&1 | tail -30</automated>
    Expected: all three tests FAIL to compile or fail at runtime (RED state) — this proves the tests exercise real behavior.
  </verify>
  <done>Three test files exist; `./gradlew compileTestKotlin` fails with unresolved-reference errors for `RecordBatch`, `SourceRecord`, `SyncMode`, or `CUSTOM_SOURCE` — OR tests compile but fail at runtime. RED state confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RecordBatch, SourceRecord, SyncMode, SchemaIntrospectionResult</name>
  <files>
    core/src/main/kotlin/riven/core/models/ingestion/adapter/RecordBatch.kt,
    core/src/main/kotlin/riven/core/models/ingestion/adapter/SourceRecord.kt,
    core/src/main/kotlin/riven/core/models/ingestion/adapter/SyncMode.kt,
    core/src/main/kotlin/riven/core/models/ingestion/adapter/SchemaIntrospectionResult.kt
  </files>
  <behavior>
    - RecordBatch: `records: List<SourceRecord>`, `nextCursor: String?` (opaque per CONTEXT.md), `hasMore: Boolean`.
    - SourceRecord: `externalId: String`, `payload: Map<String, Any?>`, `sourceMetadata: Map<String, Any?>? = null`. Neutral across Nango/Postgres/CSV.
    - SyncMode: `enum class SyncMode { POLL, CDC, PUSH, ONE_SHOT }`.
    - SchemaIntrospectionResult: `tables: List<TableSchema>`. `TableSchema(name, columns: List<ColumnSchema>)`. `ColumnSchema(name, typeLiteral: String, nullable: Boolean)`. KDoc explicitly notes Phase 3 will extend with PK/FK info (PG-07).
  </behavior>
  <action>
    Create the four files in `core/src/main/kotlin/riven/core/models/ingestion/adapter/` (new directory — Kotlin mkdir via first file write is fine; if gradle doesn't pick up, verify sourceSets).

    RecordBatch.kt:
    ```kotlin
    package riven.core.models.ingestion.adapter

    /**
     * Neutral batch result from [IngestionAdapter.fetchRecords].
     * @property nextCursor Opaque adapter-owned cursor; orchestrator round-trips it without parsing.
     *                     null = terminal (combined with hasMore=false).
     */
    data class RecordBatch(
        val records: List<SourceRecord>,
        val nextCursor: String?,
        val hasMore: Boolean,
    )
    ```

    SourceRecord.kt: neutral DTO with externalId, payload, sourceMetadata (nullable, default null). KDoc: "Shape is intentionally loose to accommodate Nango (JSON payload), Postgres (typed columns), CSV (string map), webhook (arbitrary payload). Phase 3 may narrow payload types."

    SyncMode.kt: four-value enum, top-level in `models/ingestion/adapter/` per CONTEXT.md (NOT in `enums/integration/` — the enum describes adapter capability, not persistence).

    SchemaIntrospectionResult.kt: three data classes (`SchemaIntrospectionResult`, `TableSchema`, `ColumnSchema`) in the same file. Keep shape minimal; Phase 3 extends.

    DO NOT add `@Entity`, `toModel()`, or any JPA. These are pure domain models.
  </action>
  <verify>
    <automated>cd core && ./gradlew test --tests "riven.core.models.ingestion.adapter.RecordBatchTest" --tests "riven.core.models.ingestion.adapter.SyncModeTest" 2>&1 | tail -20</automated>
    Expected: both tests PASS. RecordBatchTest + SyncModeTest green.
  </verify>
  <done>RecordBatchTest + SyncModeTest pass. Files exist with package `riven.core.models.ingestion.adapter`. No JPA annotations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CUSTOM_SOURCE to SourceType enum + verify JPA round-trip</name>
  <files>core/src/main/kotlin/riven/core/enums/integration/SourceType.kt</files>
  <behavior>
    - `SourceType.CUSTOM_SOURCE` compiles and is a valid enum constant.
    - `EntityTypeEntity(sourceType = SourceType.CUSTOM_SOURCE)` saves to H2, reloads, and round-trips the exact value.
    - No DB migration required (column is `VARCHAR(50)` with `@Enumerated(STRING)`; 13 chars < 50).
  </behavior>
  <action>
    1. Read `core/src/main/kotlin/riven/core/enums/integration/SourceType.kt`.
    2. Append `CUSTOM_SOURCE` as the final enum value, preserving existing order. Add KDoc line: `/** Custom data source (Postgres, CSV, etc.) introspected at runtime. Phase 1 foundation. */` on the value.
    3. DO NOT add a Flyway/Liquibase migration — project has no migration tooling (core/CLAUDE.md:105). The column is `VARCHAR(50)` declarative schema; no alteration needed.
    4. Verify no other switch/when exhaustiveness breaks — `grep -rn "when.*SourceType" core/src/main/kotlin/` and inspect each call site. If any `when (sourceType)` exists without `else`, add `CUSTOM_SOURCE -> {}` or `else -> {}` to preserve compile. Expectation: most call sites use `.name`/`.toString()` and don't switch; if they do, minimal branch addition.
  </action>
  <verify>
    <automated>cd core && ./gradlew test --tests "riven.core.entity.entity.SourceTypeJpaRoundTripTest" 2>&1 | tail -20</automated>
    Expected: test PASSES — H2 round-trip returns `SourceType.CUSTOM_SOURCE` exactly.
  </verify>
  <done>SourceType.kt contains CUSTOM_SOURCE. SourceTypeJpaRoundTripTest green. `./gradlew compileKotlin` succeeds (no broken `when` exhaustiveness). No migration file created.</done>
</task>

</tasks>

<verification>
After all tasks:
- `cd core && ./gradlew test --tests "riven.core.models.ingestion.adapter.*" --tests "riven.core.entity.entity.SourceTypeJpaRoundTripTest"` — all green.
- `cd core && ./gradlew compileKotlin compileTestKotlin` — no errors, no warnings from newly-exhaustive `when` blocks.
- `grep -rn "CUSTOM_SOURCE" core/src/main/kotlin/riven/core/enums/` — exactly one hit (the enum definition).
</verification>

<success_criteria>
- RecordBatch, SourceRecord, SyncMode, SchemaIntrospectionResult exist in `models/ingestion/adapter/`.
- SourceType.CUSTOM_SOURCE round-trips through JPA on EntityTypeEntity.
- No new dependencies added to build.gradle.
- Zero runtime behavior change to existing services (no imports added in `service/` tree yet — that's Plans 02+03).
</success_criteria>

<output>
Create `.planning/phases/01-adapter-foundation/01-01-SUMMARY.md` per summary.md template.
</output>
